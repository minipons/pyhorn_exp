### FeatureScript Guide: Defining Features and UI

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

This section outlines key aspects of the FeatureScript guide, focusing on defining feature types, user interface (UI) specifications, and feature output mechanisms. It also touches upon values, types, modeling, custom tables, computed part properties, imports, and debugging.

```FeatureScript
[Introduction](https://cad.onshape.com/FsDoc/intro.html)
[Defining feature types](https://cad.onshape.com/FsDoc/feature-types.html)
[Feature UI](https://cad.onshape.com/FsDoc/uispec.html)
[Feature output](https://cad.onshape.com/FsDoc/output.html)
[Values and types](https://cad.onshape.com/FsDoc/variables.html)
[Modeling](https://cad.onshape.com/FsDoc/modeling.html)
[Custom tables](https://cad.onshape.com/FsDoc/tables.html)
[Computed part properties](https://cad.onshape.com/FsDoc/computed-part-properties.html)
[Imports](https://cad.onshape.com/FsDoc/imports.html)
[Debugging in Feature Studios](https://cad.onshape.com/FsDoc/debugging-in-feature-studios.html)
```

--------------------------------

### FeatureScript Guide: Defining Features and UI

Source: https://cad.onshape.com/FsDoc/index

This section outlines key aspects of the FeatureScript guide, focusing on defining feature types, user interface (UI) specifications, and feature output mechanisms. It also touches upon values, types, modeling, custom tables, computed part properties, imports, and debugging.

```FeatureScript
[Introduction](https://cad.onshape.com/FsDoc/intro.html)
[Defining feature types](https://cad.onshape.com/FsDoc/feature-types.html)
[Feature UI](https://cad.onshape.com/FsDoc/uispec.html)
[Feature output](https://cad.onshape.com/FsDoc/output.html)
[Values and types](https://cad.onshape.com/FsDoc/variables.html)
[Modeling](https://cad.onshape.com/FsDoc/modeling.html)
[Custom tables](https://cad.onshape.com/FsDoc/tables.html)
[Computed part properties](https://cad.onshape.com/FsDoc/computed-part-properties.html)
[Imports](https://cad.onshape.com/FsDoc/imports.html)
[Debugging in Feature Studios](https://cad.onshape.com/FsDoc/debugging-in-feature-studios.html)
```

--------------------------------

### FeatureScript Guide: Defining Features and UI

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

This section outlines key aspects of the FeatureScript guide, focusing on defining feature types, user interface (UI) specifications, and feature output mechanisms. It also touches upon values, types, modeling, custom tables, computed part properties, imports, and debugging.

```FeatureScript
[Introduction](https://cad.onshape.com/FsDoc/intro.html)
[Defining feature types](https://cad.onshape.com/FsDoc/feature-types.html)
[Feature UI](https://cad.onshape.com/FsDoc/uispec.html)
[Feature output](https://cad.onshape.com/FsDoc/output.html)
[Values and types](https://cad.onshape.com/FsDoc/variables.html)
[Modeling](https://cad.onshape.com/FsDoc/modeling.html)
[Custom tables](https://cad.onshape.com/FsDoc/tables.html)
[Computed part properties](https://cad.onshape.com/FsDoc/computed-part-properties.html)
[Imports](https://cad.onshape.com/FsDoc/imports.html)
[Debugging in Feature Studios](https://cad.onshape.com/FsDoc/debugging-in-feature-studios.html)
```

--------------------------------

### FeatureScript Lambda Function Examples

Source: https://cad.onshape.com/FsDoc/top-level

Provides examples of defining lambda functions in FeatureScript using both the 'function' keyword and the '=>' shorthand.

```FeatureScript
const zero = function() { return 0; };
```

```FeatureScript
const doubled = (val) => val * 2;
```

--------------------------------

### Onshape FeatureScript Documentation Overview

Source: https://cad.onshape.com/FsDoc/relational

Provides an overview of the Onshape FeatureScript documentation, including links to tutorials, the standard library, and various guides covering the language's features and usage.

```FeatureScript
Project: /websites/cad_onshape_fsdoc

[FeatureScript](https://cad.onshape.com/FsDoc/index.html)
[Welcome to FeatureScript](https://cad.onshape.com/FsDoc/index.html)
  * [_Tutorials_](https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature.html)
  * [_Standard Library documentation_](https://cad.onshape.com/FsDoc/library.html)
  * [_Standard Library source_](https://cad.onshape.com/documents/12312312345abcabcabcdeff)


[FeatureScript guide](https://cad.onshape.com/FsDoc/intro.html)
  * [Introduction](https://cad.onshape.com/FsDoc/intro.html)
  * [Defining feature types](https://cad.onshape.com/FsDoc/feature-types.html)
  * [Feature UI](https://cad.onshape.com/FsDoc/uispec.html)
  * [Feature output](https://cad.onshape.com/FsDoc/output.html)
  * [Values and types](https://cad.onshape.com/FsDoc/variables.html)
  * [Modeling](https://cad.onshape.com/FsDoc/modeling.html)
  * [Custom tables](https://cad.onshape.com/FsDoc/tables.html)
  * [Computed part properties](https://cad.onshape.com/FsDoc/computed-part-properties.html)
  * [Imports](https://cad.onshape.com/FsDoc/imports.html)
  * [Debugging in Feature Studios](https://cad.onshape.com/FsDoc/debugging-in-feature-studios.html)


[Language reference](https://cad.onshape.com/FsDoc/tokens.html)
  * [Lexical conventions](https://cad.onshape.com/FsDoc/tokens.html)
  * [Types and type tags](https://cad.onshape.com/FsDoc/type-tags.html)
  * [Top-level constructs](https://cad.onshape.com/FsDoc/top-level.html)
  * [Syntax and semantics](https://cad.onshape.com/FsDoc/syntax.html)
  * [Annotations](https://cad.onshape.com/FsDoc/annotations.html)
  * [Exception handling](https://cad.onshape.com/FsDoc/exceptions.html)
  * [Equality and ordering](https://cad.onshape.com/FsDoc/relational.html)
```

--------------------------------

### FeatureScript Tutorials and Learning Resources

Source: https://cad.onshape.com/FsDoc/index

This section directs users to tutorials for learning FeatureScript, focusing on essential concepts and the construction of custom feature types. It provides a link to the starting tutorial for creating a slot feature.

```FeatureScript
The FeatureScript tutorials introduce the essential concepts of writing FeatureScript, while instructing you how to construct and improve your own custom feature types.
[Start FeatureScript tutorials](https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature.html)
```

--------------------------------

### FeatureScript Tutorials and Learning Resources

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

This section directs users to tutorials for learning FeatureScript, focusing on essential concepts and the construction of custom feature types. It provides a link to the starting tutorial for creating a slot feature.

```FeatureScript
The FeatureScript tutorials introduce the essential concepts of writing FeatureScript, while instructing you how to construct and improve your own custom feature types.
[Start FeatureScript tutorials](https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature.html)
```

--------------------------------

### FeatureScript Import Example

Source: https://cad.onshape.com/FsDoc/top-level

Demonstrates how to import modules in FeatureScript, including using a namespace to access imported symbols. Imports can be from tabs within the current document, other documents, or the standard library.

```FeatureScript
myNamespace::import(path : 'onshape/std/math.fs', version : '255.0');
const PI2 = myNamespace::PI * 2;
```

--------------------------------

### FeatureScript Tutorials and Learning Resources

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

This section directs users to tutorials for learning FeatureScript, focusing on essential concepts and the construction of custom feature types. It provides a link to the starting tutorial for creating a slot feature.

```FeatureScript
The FeatureScript tutorials introduce the essential concepts of writing FeatureScript, while instructing you how to construct and improve your own custom feature types.
[Start FeatureScript tutorials](https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature.html)
```

--------------------------------

### FeatureScript Annotations Example

Source: https://cad.onshape.com/FsDoc/annotations

Demonstrates the use of annotations in FeatureScript for defining user interfaces and controlling feature visibility. Annotations are key-value pairs interpreted by system software.

```FeatureScript
export enum BooleanOperationType
{
    annotation {"Name" : "Union"}
    UNION,
    annotation {"Name" : "Subtract"}
    SUBTRACTION,
    annotation {"Name" : "Intersect"}
    INTERSECTION,
    annotation {"Hidden" : true}
    SUBTRACT_COMPLEMENT
}

annotation { "Feature Type Name" : "Fillet" }
```

--------------------------------

### FeatureScript Precondition Examples

Source: https://cad.onshape.com/FsDoc/top-level

Illustrates two ways to define preconditions for FeatureScript functions: a single statement precondition and a block statement precondition.

```FeatureScript
function sqrt(n is number) returns number
precondition n >= 0;
{
    ... 
}
```

```FeatureScript
function makeArray(n is number) returns array
precondition
{
    isInteger(n);
    n > 0;
}
{
    ...
}
```

--------------------------------

### FeatureScript Function Call Example

Source: https://cad.onshape.com/FsDoc/variables

Shows how to call a previously declared lambda function with arguments and retrieve its return value.

```FeatureScript
var sum = f(1);
```

--------------------------------

### UIHint Annotation Example

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates how to use the UIHint annotation to control the rendering of a boolean parameter as an opposite direction button.

```FeatureScript
annotation { "Name" : "Flip", "UIHint" : UIHint.OPPOSITE_DIRECTION }
definition.isFlipped is boolean;
```

--------------------------------

### FeatureScript Container Access Examples

Source: https://cad.onshape.com/FsDoc/syntax

Demonstrates accessing components of container objects like arrays and maps using bracket and dot notation in FeatureScript.

```FeatureScript
x[];  // box access
x[1]; // array or map access
x.y;  // map access, x['y']
x.y.z; // map access, (x.y).z
```

--------------------------------

### FeatureScript Type Expression Examples

Source: https://cad.onshape.com/FsDoc/syntax

Illustrates the usage of type expressions in FeatureScript, including casting with 'as' and type checking with 'is'.

```FeatureScript
4 as number;
vector(0,0) as array;
x is Vector;
```

--------------------------------

### Onshape Custom Feature Library Examples

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

Highlights the Onshape Custom Feature Library, which hosts over 50 community-contributed custom features. It mentions that each feature includes usage descriptions and publicly available source code for viewing or modification.

```FeatureScript
Onshape's Custom Feature Library contains 50+ high-quality, general-use custom features from the Onshape community. Each is documented with a description of how to use it, and each has public source code that can be viewed (or copied and modified).
[View custom feature examples](https://www.onshape.com/en/features/custom-features)
```

--------------------------------

### Onshape Custom Feature Library Examples

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

Highlights the Onshape Custom Feature Library, which hosts over 50 community-contributed custom features. It mentions that each feature includes usage descriptions and publicly available source code for viewing or modification.

```FeatureScript
Onshape's Custom Feature Library contains 50+ high-quality, general-use custom features from the Onshape community. Each is documented with a description of how to use it, and each has public source code that can be viewed (or copied and modified).
[View custom feature examples](https://www.onshape.com/en/features/custom-features)
```

--------------------------------

### FeatureScript Math Operations with Units

Source: https://cad.onshape.com/FsDoc/intro

Shows examples of FeatureScript's operator overloads for ValueWithUnits, enabling straightforward mathematical operations while maintaining unit integrity and detecting mismatches.

```FeatureScript
var squareArea = (3 * meter + 3 * inch)^2;                              // Has area units

var pendulumPeriod = 2 * PI * sqrt(armLength / (9.8 * meter/second^2)); // Has time units

var numberOfBricks = floor(wallLength / brickLength);                   // Has no units

var nonsense = (3 * meter) + (3 * degree);                              // Throws an error from unit mismatch

```

--------------------------------

### Onshape Custom Feature Library Examples

Source: https://cad.onshape.com/FsDoc/index

Highlights the Onshape Custom Feature Library, which hosts over 50 community-contributed custom features. It mentions that each feature includes usage descriptions and publicly available source code for viewing or modification.

```FeatureScript
Onshape's Custom Feature Library contains 50+ high-quality, general-use custom features from the Onshape community. Each is documented with a description of how to use it, and each has public source code that can be viewed (or copied and modified).
[View custom feature examples](https://www.onshape.com/en/features/custom-features)
```

--------------------------------

### Onshape FeatureScript Evaluation Functions

Source: https://cad.onshape.com/FsDoc/modeling

Evaluation functions measure geometry and topology within a context. They start with 'ev' and accept a Context and a map for measurements. Examples include evBox3d for bounding boxes and evVolume for volumes.

```FeatureScript
evBox3d(context, map)
evVolume(context, map)
evVertexPoint(context, map)
```

--------------------------------

### Full Coordinate System and Sketch Plane Creation

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

A comprehensive example demonstrating the creation of a coordinate system and sketch plane by first evaluating endpoint positions and calculating axis directions.

```FeatureScript
var endPoints is Query = qAdjacent(definition.slotPath, AdjacencyType.VERTEX, EntityType.VERTEX);

var startPosition is Vector = evVertexPoint(context, {
        "vertex" : qNthElement(endPoints, 0)
});
var endPosition is Vector = evVertexPoint(context, {
        "vertex" : qNthElement(endPoints, 1)
});

var xDirection is Vector = normalize(endPosition - startPosition);
var zDirection is Vector = evOwnerSketchPlane(context, {
        "entity" : definition.slotPath
}).normal;

var cSys is CoordSystem = coordSystem(startPosition, xDirection, zDirection);
var sketchPlane is Plane = plane(cSys);
```

--------------------------------

### Onshape Standard Library Documentation and Source

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

Details the Onshape Standard Library, which includes Onshape's built-in features (Extrude, Fillet) and numerous geometry-focused functions. It provides links to both the documentation for reference and the open-source code for examples and modification.

```FeatureScript
The Onshape Standard library provides all of Onshape's features (like Extrude and Fillet), as well as a large number of functions and types designed to work with geometry and help build custom features. All functions in the Standard Library are imported by default into all new Feature Studios.
### Documentation
The documentation for the standard library is a useful reference for the functions and types you will use when writing FeatureScript.
[View Standard Library documentation](https://cad.onshape.com/FsDoc/library.html)
### Source code
The Onshape Standard Library is open-source and freely viewable in a public Onshape document named "std". Viewing the library's source is often useful for finding examples of functions and types in action. You can search through the document by pressing the search button on the top navbar.
[View Standard Library source code](https://cad.onshape.com/documents/12312312345abcabcabcdeff)
```

--------------------------------

### Recursive Data Structure Example

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates how assignment works with self-referential structures in Cadence, clarifying that it creates a copy rather than a true recursive link.

```cadence
var recursive = {a:0};
recursive.a = recursive;

// The value of recursive.a is a copy of the value of recursive prior to assignment
// recursive == {a : {a : 0}}

```

--------------------------------

### FeatureScript Predicate Example

Source: https://cad.onshape.com/FsDoc/top-level

Demonstrates the structure and behavior of a FeatureScript predicate, which returns a boolean and restricts side effects and variable declarations.

```FeatureScript
predicate canBeUsed(x, y)
{
    isUseful(x); // Expression statement
    if (y is array) // Not an expression statement, but controls whether the following code block is evaluated
    {
        for (element in y) // Creates loop variable "element"
        {
            element is UsefulType; // Expression statement evaluated on every element in y
        }
    }
}
```

--------------------------------

### FeatureScript isAnything Precondition

Source: https://cad.onshape.com/FsDoc/uispec

Explains the use of the `isAnything` precondition in FeatureScript, which accepts any parameter expression, including booleans, strings, vectors, and lambda functions. Shows examples of default values for `isAnything`.

```FeatureScript
"57"
"&quot;My string&quot;"
true
function(){ return &quot;Hello, world!&quot; }

```

--------------------------------

### FeatureScript Array Literals and Access

Source: https://cad.onshape.com/FsDoc/variables

Illustrates how to declare array literals, access elements using zero-based indexing, and handle out-of-range access. Includes examples of safe navigation.

```FeatureScript
var empty = [];
var numbers = [1, 2, 3];
var things = ["1", 2, ["inner", "array"]];

var x = numbers[0];         // x is 1
x = things[2][1];           // x is "array"

var index = 1;
x = numbers[index];         // x is 2

x = empty[0];               // Throws out-of-range error

things[2] = 42;             // things is now ["1", 2, 42]

var x = undefined;
var z = x?[1];              // z is undefined
```

--------------------------------

### FeatureScript Print and Println Functions

Source: https://cad.onshape.com/FsDoc/output

Demonstrates the usage of `print` and `println` functions in FeatureScript for outputting string literals and variable values to the notices flyout. Shows how `println` uses `toString()` and how to get raw string representation.

```FeatureScript
print("x is ");     // Prints the string "x is "
println(x);         // Prints a string representation of the value x, followed by a newline
```

```FeatureScript
println(1 * meter);         // Prints "1 meter"
println("" ~ (1 * meter));  // Prints "ValueWithUnits : { \"unit\" : UnitSpec : { \"meter\" : 1 } , \"value\" : 1 }"
```

--------------------------------

### Enumeration Declaration Example

Source: https://cad.onshape.com/FsDoc/top-level

Demonstrates the declaration of an enumeration type 'Weekend' with its members SATURDAY and SUNDAY, and how to reference its members using dot syntax.

```featurescript
enum Weekend { SATURDAY, SUNDAY }
// Usage: Weekend.SATURDAY
```

--------------------------------

### Create Coordinate System

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Creates a Coordinate System using a starting position, X-axis direction, and Z-axis direction. The Y-axis is inferred from the other two.

```FeatureScript
var cSys is CoordSystem = coordSystem(startPosition, xDirection, zDirection);
```

--------------------------------

### FeatureScript Variable and Constant Declaration

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates the declaration and usage of variables (mutable) and constants (immutable) in FeatureScript. Includes examples of assignment and type constraints.

```FeatureScript
var x = 1;
const y = 2;

x = 3; // succeeds

y = 3; // error: cannot assign to constant y
```

--------------------------------

### FeatureScript Table Input Parameters

Source: https://cad.onshape.com/FsDoc/tables

Demonstrates how to define input parameters for custom tables in FeatureScript, similar to custom features. It shows examples of boolean and quantity parameters with annotations for user-facing names.

```FeatureScript
precondition
{
    annotation { "Name" : "Minimum fillet radius" }
    isLength(definition.minimumFilletRadius, NONNEGATIVE_ZERO_DEFAULT_LENGTH_BOUNDS);

    annotation { "Name" : "Calculate size" }
    definition.calculateSize is boolean;
}
```

--------------------------------

### Onshape Standard Library Documentation and Source

Source: https://cad.onshape.com/FsDoc/index

Details the Onshape Standard Library, which includes Onshape's built-in features (Extrude, Fillet) and numerous geometry-focused functions. It provides links to both the documentation for reference and the open-source code for examples and modification.

```FeatureScript
The Onshape Standard library provides all of Onshape's features (like Extrude and Fillet), as well as a large number of functions and types designed to work with geometry and help build custom features. All functions in the Standard Library are imported by default into all new Feature Studios.
### Documentation
The documentation for the standard library is a useful reference for the functions and types you will use when writing FeatureScript.
[View Standard Library documentation](https://cad.onshape.com/FsDoc/library.html)
### Source code
The Onshape Standard Library is open-source and freely viewable in a public Onshape document named "std". Viewing the library's source is often useful for finding examples of functions and types in action. You can search through the document by pressing the search button on the top navbar.
[View Standard Library source code](https://cad.onshape.com/documents/12312312345abcabcabcdeff)
```

--------------------------------

### Onshape Standard Library Documentation and Source

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

Details the Onshape Standard Library, which includes Onshape's built-in features (Extrude, Fillet) and numerous geometry-focused functions. It provides links to both the documentation for reference and the open-source code for examples and modification.

```FeatureScript
The Onshape Standard library provides all of Onshape's features (like Extrude and Fillet), as well as a large number of functions and types designed to work with geometry and help build custom features. All functions in the Standard Library are imported by default into all new Feature Studios.
### Documentation
The documentation for the standard library is a useful reference for the functions and types you will use when writing FeatureScript.
[View Standard Library documentation](https://cad.onshape.com/FsDoc/library.html)
### Source code
The Onshape Standard Library is open-source and freely viewable in a public Onshape document named "std". Viewing the library's source is often useful for finding examples of functions and types in action. You can search through the document by pressing the search button on the top navbar.
[View Standard Library source code](https://cad.onshape.com/documents/12312312345abcabcabcdeff)
```

--------------------------------

### FeatureScript Function Definition

Source: https://cad.onshape.com/FsDoc/top-level

Shows the basic syntax for defining a function in FeatureScript. Functions take arguments, execute statements, and return a value. The example defines a function to calculate the fourth power of a number.

```FeatureScript
function fourthPower(x)
{
    const square = x * x;
    return square * square;
}
```

--------------------------------

### Conditional Visibility with If Statements

Source: https://cad.onshape.com/FsDoc/uispec

Shows how to use 'if' statements to conditionally display parameters based on the values of other parameters, with examples of comparisons and logical operators.

```FeatureScript
precondition
{
    definition.useCustomLength is boolean;
    if (definition.useCustomLength)
        isLength(definition.customLength, LENGTH_BOUNDS);
}
```

--------------------------------

### FeatureScript Array Manipulation with Containers Module

Source: https://cad.onshape.com/FsDoc/variables

Shows common array operations like getting the size, creating arrays, appending, concatenating, and resizing using functions from the 'containers' module.

```FeatureScript
var s = size(numbers);              // s is 3
var arr = makeArray(3);             // arr is [undefined, undefined, undefined]
var arrFalse = makeArray(3, false); // arrFalse is [false, false, false]

numbers = append(numbers, 4);                   // numbers is [1, 2, 3, 4]
numbers = concatenateArrays([numbers, [5, 6]]); // numbers is [1, 2, 3, 4, 5, 6]
numbers = resize(numbers, 3);                   // numbers is [1, 2, 3]
```

--------------------------------

### FeatureScript Sort with Lambda Function

Source: https://cad.onshape.com/FsDoc/variables

Provides an example of using a lambda function as a comparison callback for the standard library's sort function, sorting edges by length.

```FeatureScript
var edgeQuery is Query = qEverything(EntityType.EDGE);
var edges is array = evaluateQuery(context, edgeQuery);

var sortedEdges = sort(edges, function(edge1, edge2) {
    return evLength(context, edge1) - 
           evLength(context, edge2);
});
```

--------------------------------

### FeatureScript Map Assignment Example

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates how assigning a map to a new variable creates a deep copy, preventing mutations in one variable from affecting the other. It also shows structural equality.

```FeatureScript
var a = { 0 : false, 1 : true }; // A map with two entries
var b = a;
a == b; // Structural equality, not physical equality
a[0] = true;
a != b; // The map in b did not change.
```

--------------------------------

### FeatureScript Type Definition and Comparison

Source: https://cad.onshape.com/FsDoc/relational

Illustrates how to define custom types using `typecheck` and how these custom types affect ordering. It shows an example where a custom type influences the comparison between values.

```FeatureScript
predicate anything(x) { true; }
type Anything typecheck anything;

// Example comparison:
// 0 comes before true as Anything
// true as Anything comes before 0 as Anything
```

--------------------------------

### FeatureScript Map Manipulation with Containers Module

Source: https://cad.onshape.com/FsDoc/variables

Shows how to check if a value exists in a map and get the size of a map using functions from the 'containers' module.

```FeatureScript
s = size(map2)                  // s is 1
x = isValueIn("two", map3);     // x is true
```

--------------------------------

### Define Feature Type Example

Source: https://cad.onshape.com/FsDoc/feature-types

Demonstrates the definition of a custom FeatureScript feature type, 'Fillet Everything'. It includes a precondition for a fillet radius parameter and uses the opFillet operation to fillet all edges of a solid body. The execution context and regeneration behavior are also highlighted.

```FeatureScript
annotation { "Feature Type Name" : "Fillet Everything" }
export const filletEverything = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Fillet radius" }
        isLength(definition.filletRadius, BLEND_BOUNDS);
    }
    {
        opFillet(context, id + "fillet1", {
                "entities" : qBodyType(qEverything(EntityType.EDGE), BodyType.SOLID),
                "radius" : definition.filletRadius
        });
    });

```

--------------------------------

### FeatureScript Query Filters

Source: https://cad.onshape.com/FsDoc/uispec

Illustrates how to define filters for queries in FeatureScript, specifying allowed entity types and combining them using logical operators. Includes an example for selecting sketch circles or arcs.

```FeatureScript
annotation { "Name" : "Round things",
             "Filter" : (GeometryType.CIRCLE || GeometryType.ARC) && SketchObject.YES,
             "MaxNumberOfPicks" : 2 }
definition.roundThings is Query;

```

--------------------------------

### Creating Arcs with skArc in FeatureScript

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Demonstrates the usage of the skArc function to create arcs for the slot bumps. It shows how to calculate start, mid, and end points using previously defined variables and how to ensure unique sketch IDs.

```FeatureScript
skArc(sketch1, "arc_0_" ~ side, {
        "start" : vector(bumpDistance, (definition.width / 2) * side),
        "mid" : vector(bumpDistance + bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
        "end" : vector(bumpDistance + bumpWidth, (definition.width / 2) * side)
});

skArc(sketch1, "arc_1_" ~ side, {
        "start" : vector(slotLength - bumpDistance, (definition.width / 2) * side),
        "mid" : vector(slotLength - bumpDistance - bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
        "end" : vector(slotLength - bumpDistance - bumpWidth, (definition.width / 2) * side)
});
```

--------------------------------

### Importing External Data (CSV Example)

Source: https://cad.onshape.com/FsDoc/imports

Demonstrates how to import data from an external CSV file into FeatureScript. It shows accessing the BLOB_DATA map and specifically the csvData array to retrieve data, assuming a multi-record CSV.

```FeatureScript
MyData::import(path : "...", version : "...");
...
const firstEntry = MyData::BLOB_DATA.csvData[0][0];
...
```

--------------------------------

### Define Slot Length and Solve Sketch

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Calculates the length of a slot based on start and end positions and includes the sketch solving step, preparing for entity creation.

```FeatureScript
var sketch1 = newSketchOnPlane(context, id + "sketch1", {
        "sketchPlane" : sketchPlane
});
var slotLength is ValueWithUnits = norm(endPosition - startPosition);

skSolve(sketch1);
```

--------------------------------

### Extrude Sketch Region

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Extrudes a selected sketch region to create a 3D body. It specifies the entities to extrude, the direction of extrusion, and the start and end bounds.

```FeatureScript
opExtrude(context, id + "extrude1", {
      "entities" : regionToExtrude,
      "direction" : zDirection,
      "endBound" : BoundingType.THROUGH_ALL,
      "startBound" : BoundingType.THROUGH_ALL
});
```

--------------------------------

### FeatureScript Box for Mutable State Example

Source: https://cad.onshape.com/FsDoc/type-tags

Illustrates the use of a 'box' to create a mutable container, enabling a function to maintain and update an internal counter across multiple calls. This allows changes to 'escape' their scope.

```FeatureScript
function counter() returns function
{
    const b = new box(0);
    return function() { b[] += 1; return b[]; };
}
```

--------------------------------

### Onshape FeatureScript Introduction and Overview

Source: https://cad.onshape.com/FsDoc/index

Provides an introduction to FeatureScript, highlighting its purpose as a programming language for 3D parametric modeling within Onshape. It explains its integration with Part Studio modeling, geometric references, parametric tools, and a 3D math-focused type system. It also mentions the use of FeatureScript for custom table types and extending Onshape's functionality.

```FeatureScript
FeatureScript is a new programming language designed by Onshape for building and working with 3D parametric models. The language is built into Onshape from the ground up, providing the foundation of Part Studio modeling, including robust geometric references, powerful parametric tools, and a type system with types built for math in three dimensions.
The standard feature types in Onshape (like Extrude, Fillet, and Helix) are already written as FeatureScript functions by our developers. Custom feature types extend this same mechanism to all users of Onshape. FeatureScript can also be used to write [custom table types](https://cad.onshape.com/FsDoc/tables.html) that gather and process data from a Part Studio.
FeatureScript can be added to any new or existing Onshape document by creating a Feature Studio. This short video shows how to create and use a new custom feature type:
Your browser does not support the video tag.
```

--------------------------------

### Onshape FeatureScript Introduction and Overview

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

Provides an introduction to FeatureScript, highlighting its purpose as a programming language for 3D parametric modeling within Onshape. It explains its integration with Part Studio modeling, geometric references, parametric tools, and a 3D math-focused type system. It also mentions the use of FeatureScript for custom table types and extending Onshape's functionality.

```FeatureScript
FeatureScript is a new programming language designed by Onshape for building and working with 3D parametric models. The language is built into Onshape from the ground up, providing the foundation of Part Studio modeling, including robust geometric references, powerful parametric tools, and a type system with types built for math in three dimensions.
The standard feature types in Onshape (like Extrude, Fillet, and Helix) are already written as FeatureScript functions by our developers. Custom feature types extend this same mechanism to all users of Onshape. FeatureScript can also be used to write [custom table types](https://cad.onshape.com/FsDoc/tables.html) that gather and process data from a Part Studio.
FeatureScript can be added to any new or existing Onshape document by creating a Feature Studio. This short video shows how to create and use a new custom feature type:
Your browser does not support the video tag.
```

--------------------------------

### FeatureScript Array Parameter Definition and Usage

Source: https://cad.onshape.com/FsDoc/uispec

Defines an array parameter named 'profiles' with custom item names and labels. It then iterates through the array, accessing inner parameters like 'width' and 'isSquare', and includes examples of array data structure and access methods.

```FeatureScript
annotation { "Name" : "Profiles",
    "Item name" : "Profile",
    "Item label template" : "Profile (#width)" }
definition.profiles is array;


for (var profile in definition.profiles)
{
    annotation { "Name" : "Width" }
    isLength(profile.width, LENGTH_BOUNDS);  

    annotation { "Name" : "Make square" }
    profile.isSquare is boolean;
}
```

```JSON
[
    { "width" : 0.9 * inch, "isSquare" : false },
    { "width" : 1.2 * inch, "isSquare" : true }
]
```

--------------------------------

### Onshape FeatureScript Introduction and Overview

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

Provides an introduction to FeatureScript, highlighting its purpose as a programming language for 3D parametric modeling within Onshape. It explains its integration with Part Studio modeling, geometric references, parametric tools, and a 3D math-focused type system. It also mentions the use of FeatureScript for custom table types and extending Onshape's functionality.

```FeatureScript
FeatureScript is a new programming language designed by Onshape for building and working with 3D parametric models. The language is built into Onshape from the ground up, providing the foundation of Part Studio modeling, including robust geometric references, powerful parametric tools, and a type system with types built for math in three dimensions.
The standard feature types in Onshape (like Extrude, Fillet, and Helix) are already written as FeatureScript functions by our developers. Custom feature types extend this same mechanism to all users of Onshape. FeatureScript can also be used to write [custom table types](https://cad.onshape.com/FsDoc/tables.html) that gather and process data from a Part Studio.
FeatureScript can be added to any new or existing Onshape document by creating a Feature Studio. This short video shows how to create and use a new custom feature type:
Your browser does not support the video tag.
```

--------------------------------

### FeatureScript Timers for Performance Profiling

Source: https://cad.onshape.com/FsDoc/output

Explains the use of `startTimer` and `printTimer` in FeatureScript for basic performance profiling. Demonstrates simple usage and how to use named timers for multiple concurrent profiling sessions.

```FeatureScript
startTimer();
possiblyExpensiveFunction();
printTimer();
```

```FeatureScript
startTimer("overall");
expensiveCall1();
for (var x in someRange)
{
    startTimer("individual call"); // resets the timer
    expensiveCall2();
    printTimer("individual call");
}
printTimer("overall");
```

--------------------------------

### Onshape FeatureScript Language Reference

Source: https://cad.onshape.com/FsDoc/syntax

Provides details on FeatureScript's lexical conventions, types, top-level constructs, syntax, semantics, annotations, exception handling, and relational operations.

```APIDOC
FeatureScript Language Reference:

Lexical Conventions:
  - Details on valid characters, identifiers, keywords, and comments.

Types and Type Tags:
  - Explanation of built-in types (e.g., number, boolean, string, vector, matrix) and how to use type tags.

Top-level Constructs:
  - Structure of FeatureScript programs, including functions, variables, and modules.

Syntax and Semantics:
  - Describes the grammar of FeatureScript, expression evaluation order, and type checking rules.
  - Example: `x[f1()](f2(), f3())` evaluates `x[f1()]` first, then `f2()`, then `f3()`.
  - Type checking: System may warn for obvious type mismatches or raise exceptions during execution.

Annotations:
  - How to use annotations to provide metadata or control behavior.

Exception Handling:
  - Mechanisms for handling runtime errors and exceptions.

Equality and Ordering:
  - Defines how equality and ordering comparisons work for different types.
```

--------------------------------

### FeatureScript Language Reference Overview

Source: https://cad.onshape.com/FsDoc/intro

Provides an overview of the FeatureScript language reference, covering lexical conventions, types, syntax, annotations, exception handling, and equality.

```FeatureScript
Lexical conventions
Types and type tags
Top-level constructs
Syntax and semantics
Annotations
Exception handling
Equality and ordering
```

--------------------------------

### FeatureScript Core Concepts

Source: https://cad.onshape.com/FsDoc/intro

Explains the fundamental concepts of FeatureScript in relation to Onshape's Part Studios, including feature types as functions, features as function calls, and Part Studios as build functions.

```FeatureScript
A feature type is a FeatureScript function.
Custom feature types are defined in Feature Studios.
A feature in the feature tree is a function call.
Feature parameters are contained in the function's arguments.
A Part Studio defines a build function, containing a function call for every feature.
The geometry of a Part Studio is the output of the build function.
Part Studio regeneration is execution of the build function.
The rollback bar is a return statement in the build function.
```

--------------------------------

### FeatureScript Language Reference and Syntax

Source: https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature

Covers the core language reference for FeatureScript, including lexical conventions, types, top-level constructs, syntax, semantics, annotations, exception handling, and relational operators. It provides links to specific documentation pages for each topic.

```FeatureScript
[Lexical conventions](https://cad.onshape.com/FsDoc/tokens.html)
[Types and type tags](https://cad.onshape.com/FsDoc/type-tags.html)
[Top-level constructs](https://cad.onshape.com/FsDoc/top-level.html)
[Syntax and semantics](https://cad.onshape.com/FsDoc/syntax.html)
[Annotations](https://cad.onshape.com/FsDoc/annotations.html)
[Exception handling](https://cad.onshape.com/FsDoc/exceptions.html)
[Equality and ordering](https://cad.onshape.com/FsDoc/relational.html)
```

--------------------------------

### FeatureScript Language Reference and Syntax

Source: https://cad.onshape.com/FsDoc/index

Covers the core language reference for FeatureScript, including lexical conventions, types, top-level constructs, syntax, semantics, annotations, exception handling, and relational operators. It provides links to specific documentation pages for each topic.

```FeatureScript
[Lexical conventions](https://cad.onshape.com/FsDoc/tokens.html)
[Types and type tags](https://cad.onshape.com/FsDoc/type-tags.html)
[Top-level constructs](https://cad.onshape.com/FsDoc/top-level.html)
[Syntax and semantics](https://cad.onshape.com/FsDoc/syntax.html)
[Annotations](https://cad.onshape.com/FsDoc/annotations.html)
[Exception handling](https://cad.onshape.com/FsDoc/exceptions.html)
[Equality and ordering](https://cad.onshape.com/FsDoc/relational.html)
```

--------------------------------

### Importing a Part Studio with Instantiator

Source: https://cad.onshape.com/FsDoc/imports

Illustrates how to import a Part Studio into FeatureScript using the instantiator module. This allows leveraging existing Part Studio geometry within a custom feature, including transforming the imported bodies.

```FeatureScript
MyThing::import(path : "", version : "");

annotation {"Feature Type Name" : "Add thing"}
export const addThing = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation {"Name" : "Mounting mate connector", "Filter" : BodyType.MATE_CONNECTOR, "MaxNumberOfPicks" : 1}
        definition.mountingConnector is Query;
    }
    {
        var thingTransform is Transform = toWorld(evMateConnector(context, {
               "mateConnector" : definition.mountingConnector
        }));

        var instantiator = newInstantiator(id + "instantiate");
        addInstance(instantiator, MyThing::build, { "transform" : thingTransform });
        instantiate(context, instantiator);
    });

```

--------------------------------

### Onshape FeatureScript Syntax and Semantics

Source: https://cad.onshape.com/FsDoc/syntax

Explains the syntax and evaluation rules of FeatureScript, noting its similarity to JavaScript and C-like languages. Covers type checking and expression evaluation order.

```FeatureScript
// FeatureScript syntax is similar to JavaScript and other C-like languages.

// Type checking: The system may warn for obvious type mismatches or raise exceptions.
// Example: Using a number where a boolean is expected might trigger a warning.
let condition: boolean = 0; // Potential warning

// Expression evaluation order: Components of an expression execute from left to right.
// Example: In `x[f1()](f2(), f3())`, the order of execution is `x[f1()]`, then `f2()`, then `f3()`.
let result = x[f1()](f2(), f3());

```

--------------------------------

### FeatureScript Language Reference and Syntax

Source: https://cad.onshape.com/FsDoc/GBTErrorStringEnum

Covers the core language reference for FeatureScript, including lexical conventions, types, top-level constructs, syntax, semantics, annotations, exception handling, and relational operators. It provides links to specific documentation pages for each topic.

```FeatureScript
[Lexical conventions](https://cad.onshape.com/FsDoc/tokens.html)
[Types and type tags](https://cad.onshape.com/FsDoc/type-tags.html)
[Top-level constructs](https://cad.onshape.com/FsDoc/top-level.html)
[Syntax and semantics](https://cad.onshape.com/FsDoc/syntax.html)
[Annotations](https://cad.onshape.com/FsDoc/annotations.html)
[Exception handling](https://cad.onshape.com/FsDoc/exceptions.html)
[Equality and ordering](https://cad.onshape.com/FsDoc/relational.html)
```

--------------------------------

### Instantiate Parts with Mate Connectors

Source: https://cad.onshape.com/FsDoc/imports

Modifies the part query to include mate connectors of selected parts before instantiation.

```FeatureScript
definition.partStudio.partQuery = qUnion([
        definition.partStudio.partQuery,
        qMateConnectorsOfParts(definition.partStudio.partQuery)
    ]);
addInstance(instantiator, definition.partStudio);

```

--------------------------------

### Create a Basic Sketch

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Demonstrates the fundamental steps to create a new sketch in FeatureScript, including defining the sketch plane and solving the sketch.

```FeatureScript
var sketch1 = newSketch(context, id + "sketch1", {
        "sketchPlane" : qCreatedBy(makeId("Top"), EntityType.FACE)
});
// Create sketch entities here
skSolve(sketch1);
```

--------------------------------

### FeatureScript Parameter Expressions

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates various valid parameter expressions in FeatureScript, including mathematical operations, unit conversions, variable usage, array indexing, ternary operators, lambda functions, and vector math.

```FeatureScript
6
22/7 mm
sqrt(1 + sin(61deg)) / 8
(round(#myWidth, .1mm) + 2 inches)
[3, 1, 4, 1, 5][#index]
(#myNum % 2 == 0) ? #myNum / 2 : 3 * #myNum + 1
(function(x) { return (x^2 + 1) in; })(1)

```

--------------------------------

### FeatureScript Debugging: Analysis and Execution Feedback

Source: https://cad.onshape.com/FsDoc/debugging-in-feature-studios

This section details the two primary methods for debugging FeatureScript code in Onshape: static analysis and runtime execution. Analysis provides inline feedback as you type, catching parse errors, semantic warnings, and precondition issues. Execution feedback, obtained after committing code and regenerating Part Studios, includes printed output, runtime exceptions, and thrown exceptions like regenErrors.

```FeatureScript
FeatureScript analysis provides inline feedback including:
  * Parse errors for code that does not match FeatureScript's grammar
  * Semantic warnings for e.g. a variable or function call name that doesn't match anything visibly defined
  * Precondition analysis warnings for a feature precondition that doesn't properly specify the [feature UI](https://cad.onshape.com/FsDoc/uispec.html)
  * Documentation extracted from source comments and shown when hovering on symbols

FeatureScript execution feedback includes:
  * Printed [output](https://cad.onshape.com/FsDoc/output.html) from functions like `print`, `println`, `printTimer`, and `debug`
  * Language runtime [exceptions](https://cad.onshape.com/FsDoc/exceptions.html) like dereferencing `undefined` or dividing by zero
  * Thrown exceptions (including [regenErrors](https://cad.onshape.com/FsDoc/library.html#module-error.fs), which appear as infos)
```

--------------------------------

### FeatureScript Box Allocation and Access

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates the creation of boxes with initial values and how to read or write the value contained within a box using the `[]` operator.

```FeatureScript
var b = new box(3);
var c = new box({x:0,y:1});
c[] = b[] + c[].y;
var d = undefined;
var x = d?[]; // x will be undefined
var y = b?[]; // y == 3
```

--------------------------------

### Place Image with FeatureScript

Source: https://cad.onshape.com/FsDoc/imports

Demonstrates how to place an image into a Part Studio using the skImage function. It requires ImageData and image width as inputs.

```typescript
annotation { "Feature Type Name" : "Place image" }
export const placeImage = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Image" }
        definition.image is ImageData;

        annotation { "Name" : "Image width" }
        isLength(definition.imageWidth, LENGTH_BOUNDS);
    }
    {
        var sketch1 = newSketch(context, id + "sketch1", {
                "sketchPlane" : qCreatedBy(makeId("Top"), EntityType.FACE)
        });
        skImage(sketch1, "image1", {
                "blobInfo" : definition.image,
                "firstCorner" : vector(0 * inch, 0 * inch),
                "secondCorner" : vector(definition.imageWidth, 0 * inch) // calculate height automatically
        });
        skSolve(sketch1);
    });

```

--------------------------------

### FeatureScript Map Literals and Key Handling

Source: https://cad.onshape.com/FsDoc/type-tags

Shows various ways to create maps using literals, including handling duplicate keys, using variable values as keys, and the effect of storing undefined values.

```FeatureScript
{}
{count:75, category:"chipmunks"}
var a = "hello, world";
{a:0, a:1} // {a:0, a:1}.["a"] == 1
{a:false, (a):true} // {a:false, (a):true}["hello, world"] == true
{{a:false}:true} // maps can be keys
{a:true,a:undefined} // same as empty map
var x = undefined;
var y = x?.k; // y will be undefined
var z = {count:75, category:"chipmunks"};
var k = z?.count; // k == 75
```

--------------------------------

### FeatureScript Imports

Source: https://cad.onshape.com/FsDoc/imports

Demonstrates how to import FeatureScript code or data from other Onshape tabs. Supports workspace and linked document imports, with optional namespacing.

```FeatureScript
// Workspace import
import(path : "990d0d558752560035c1bc8e", version : "e83d3c3d23dea63825b44d09");

// Linked document import
import(path : "62bfa9d164ac3029984f7b53/c28fe04f33cbfe2c69f4e894/aa388ed6ebc848668b717010", version : "56c4b769218439ffa1a4699e");

```

```FeatureScript
var result = importedFunction();
```

```FeatureScript
MyFunctions::import(...);
...
x = MyFunctions::importedFunction();
```

```FeatureScript
export function myFunction(x) {...}
export enum myEnum {...}
export type myType typecheck p;
export predicate myPredicate(value) {...}
```

```FeatureScript
export import(path : "onshape/std/tool.fs", version : "");
```

--------------------------------

### Feature Studios and Modularity

Source: https://cad.onshape.com/FsDoc/intro

Details how Feature Studios are used to define custom features and other user-defined functions within Onshape documents, emphasizing modularity and code reuse through imports.

```FeatureScript
Feature Studios define feature types and other user-defined functions.
Tabs like Feature Studios, Part Studios, and data tabs are 'modules'.
Modules can be imported into Feature Studios from the same or linked documents.
This enables code and data reuse.
Feature Studios leverage Onshape's version control and sharing systems.
```

--------------------------------

### FeatureScript String Literals and Manipulation

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates the creation of string literals using single and double quotes, escape sequences, and basic string replacement using the 'string' module.

```FeatureScript
var message1 = "Hello, world!";
var message2 = 'FeatureScript,\nnot FutureScript';
var message3 = "Smile \u263A";

var str = replace("The answer to everything is 42.", "[1-9]+", "smarter features");
```

--------------------------------

### FeatureScript Geometric Calculations with Vectors and Transforms

Source: https://cad.onshape.com/FsDoc/intro

Demonstrates basic arithmetic operations on Vector and Transform types in FeatureScript, showcasing how operator overloads simplify geometric computations.

```FeatureScript
var v is Vector = vector(1, 2) + vector(3, 4);
var transformedPoint = transform2 * transform1 * originalPoint;

```

--------------------------------

### Create Lumber Constructor

Source: https://cad.onshape.com/FsDoc/variables

Provides a constructor function 'lumber' to create instances of the 'Lumber' custom type. It takes 'size' and 'length' as arguments and returns a map with these properties, tagged as 'Lumber' using the 'as' keyword.

```typescript
export function lumber(size is LumberSize, length is ValueWithUnits)
{
    return { "size" : size, "length" : length } as Lumber;
}
```

--------------------------------

### Onshape FeatureScript Lookup Table Definition and Usage

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates the definition of a lookup table for parameter selection, including units and nested entries. It also shows how to declare and use a lookup table within a feature definition.

```FeatureScript
const ml is ValueWithUnits = centimeter ^ 3;
const oz is ValueWithUnits = 29.5735 * ml;

export const sizeTable = {
        "name" : "region",
        "displayName" : "Region",
        "default" : "EU",
        "entries" : {
            "EU" : {
                "name" : "size",
                "displayName" : "Size",
                "default" : "Medium",
                "entries" : {
                    "Small" : 250 * ml,
                    "Medium" : 350 * ml,
                    "Large" : 500 * ml
                }
            },
            "US" : {
                "name" : "size",
                "displayName" : "Size",
                "default" : "Large",
                "entries" : {
                    "Medium" : 16 * oz,
                    "Large" : 32 * oz,
                    "Supersize" : 64 * oz
                }
            }
        }
    };

annotation { "Feature Type Name" : "Soda" }
export const soda = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Soda size", "Lookup Table" : sizeTable }
        definition.size is LookupTablePath;
    }
    {
        var size is ValueWithUnits = getLookupTable(sizeTable, definition.size);

        fCylinder(context, id + "cylinder1", {
                "bottomCenter" : vector(0, 0, 0) * inch,
                "topCenter" : vector(0, 0, 1) * size ^ (1/3),
                "radius" : size ^ (1/3) / sqrt(PI)
        });
    });

```

--------------------------------

### Adding Descriptions to Features and Parameters

Source: https://cad.onshape.com/FsDoc/uispec

Illustrates how to add descriptions to features and parameters using annotation fields, supporting multi-line text and basic HTML.

```FeatureScript
annotation { "Feature Type Name" : "Slot",
             "Feature Type Description" : "Creates a rectangular slot in a selected part.<br>" ~ 
                                          "This feature is an <b>example</b>"
 }
export const myFeature = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
            annotation { "Name" : "Slot length",
                         "Description" : "Distance from the slot's end to its furthest extent along the outside of the part" }
            isLength(definition.slotLength, LENGTH_BOUNDS);
    }
...
```

--------------------------------

### FeatureScript defineFeature with Lambda

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates the usage of `defineFeature` which accepts a lambda function for defining feature behavior, including preconditions and actions.

```FeatureScript
annotation { "Feature Type Name" : "My Feature" }
export const myFeature = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        // Define the parameters of the feature type
    }
    {
        // Define the function's action
    });

```

--------------------------------

### FeatureScript Table Creation and Content

Source: https://cad.onshape.com/FsDoc/tables

Defines how to construct tables in FeatureScript, including column definitions using `tableColumnDefinition` and row construction with `tableRow`. It also outlines the different types of data that can be placed in table cells.

```FeatureScript
tableColumnDefinition(id: "internalId", name: "User Name", alignment: TableColumnAlignment.LEFT)

tableRow(columnIdToCell={
    "internalId": "String value"
    "internalId": valueWithUnits(1.23, "in")
    "internalId": templateString("diam = #d", { "d": someDiameter })
    "internalId": tableCellError("bad value", "Bad value encountered")
})

println(table("Part volumes", columns, rows));
```

--------------------------------

### FeatureScript Exception Handling with try/catch

Source: https://cad.onshape.com/FsDoc/exceptions

Demonstrates the use of 'try' statements and 'catch' blocks for handling exceptions in FeatureScript. Covers expression and statement forms, including catching specific exception values.

```FeatureScript
try (0/0)
try (f(x) - f(y))
```

```FeatureScript
try (0/0) - 0
```

```FeatureScript
try { f(x); }
try { f(x); } catch { fail(); }
try { f(x); } catch (e) { analyze(e); }
```

--------------------------------

### FeatureScript Basic Declaration

Source: https://cad.onshape.com/FsDoc/uispec

Defines the minimal structure for a FeatureScript feature, including annotations, preconditions, and the feature body. This serves as the entry point for creating custom features in Onshape.

```FeatureScript
annotation { "Feature Type Name" : "Fancy Fillet" /* May have additional options */}
export const fancyFillet = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        // Parameter specifications go here
    }
    {
        // Feature Body
    });

```

--------------------------------

### FeatureScript Lexical Conventions and Tokens

Source: https://cad.onshape.com/FsDoc/tokens

Details the rules for identifiers, keywords, operators, and comments in FeatureScript. It covers character sets, string escaping, numeric constants, and the distinction between C-style and C++-style comments. It also lists reserved keywords for various constructs.

```FeatureScript
/* FeatureScript Comments */
// Single line comments

// Identifiers: start with letter or underscore, followed by alphanumeric or underscore.
let myVariable = 10;
let _anotherVariable = "hello";

// String Literals: single or double quotes, with escape sequences like \n, \t, \uXXXX.
let message = "This is a string with \"quotes\".";
let unicodeChar = "\u20AC"; // Euro symbol

// Numeric Constants: decimal floating point, e.g., 3e9, 1.44, and 'inf' for infinity.
let bigNumber = 3e9;
let pi = 3.14159;
let infinity = inf;

// Operators:
// Unary: -, !, [], ()
// Binary: +, -, *, /, %, ^, ~, <, >, <=, >=, ==, !=, &&, ||, ??
// Assignment: =, +=, -=, *=, /=, ^=, %=
// Special: ., [], is, as

// Keywords:
// Top-level and functions: annotation, enum, export, function, import, operator, precondition, predicate, returns, type, typecheck, typeconvert
// Expressions: as, is, new
// Statements: break, const, continue, for, in, return, var, while
// Literals: false, inf, true, undefined
// Exceptions: catch, throw, try
// Reserved: assert, case, default, do, switch

// Example of operator usage:
let result = (10 + 5) * 2;
let concatenated = "Hello" ~ " " ~ "World";
let isDefined = myVariable ?? undefined;

```

--------------------------------

### Place Parts Feature

Source: https://cad.onshape.com/FsDoc/imports

Defines a FeatureScript feature to place parts from a selected Part Studio. It uses PartStudioData to instantiate selected bodies.

```FeatureScript
annotation { "Feature Type Name" : "Place parts" }
export const placeParts = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Parts" }
        definition.partStudio is PartStudioData;
    }
    {
        var instantiator = newInstantiator(id + "instantiator");
        addInstance(instantiator, definition.partStudio);
        instantiate(context, instantiator);
    });

```

--------------------------------

### FeatureScript Map Iteration

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates iterating over key-value pairs in a map using a for loop.

```FeatureScript
for (var key, value in myMap)
{
    println("Key: " ~ key ~ ", Value: " ~ value);
}
```

--------------------------------

### Create Sketch using newSketchOnPlane

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Corrects a common runtime error by using the `newSketchOnPlane` function, which expects a Query for the sketch plane, resolving precondition failures.

```FeatureScript
var sketch1 = newSketchOnPlane(context, id + "sketch1", {
        "sketchPlane" : sketchPlane
});
```

--------------------------------

### Onshape Feature Description Image Import

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates how to import and reference a description image for an Onshape custom feature. Images are uploaded as JPG, PNG, or SVG blobs and referenced using ImageNamespace::BLOB_DATA.

```javascript
ImageNamespace::import(...);

annotation {
  "Feature Type Name" : "MyFeature",
  "Description Image" : ImageNamespace::BLOB_DATA
}

export const myFeature = defineFeature(function(context is Context, id is Id, definition is map)
...
```

--------------------------------

### Onshape FeatureScript Notice Filtering

Source: https://cad.onshape.com/FsDoc/debugging-in-feature-studios

This section details the available filters for the notices pane in Onshape Feature Studio. Filters can be combined for more specific results. Searches are case-insensitive.

```APIDOC
Notice Filtering Parameters:

- :text <search_term>
  - Filters notices based on the text content.
  - Example: :text "invalid input"

- :elementid <element_id>
  - Filters notices by a specific element ID.
  - Example: :elementid "a1b2c3d4e5f6"

- :tabname <tab_name>
  - Filters notices for tab names that match the search string.
  - Use quotes for exact matches: :tabname "Part Studio 1"
  - Example: :tabname part studio

- :currenttab
  - Filters notices to only show those for the current tab.

- :minlevel <level>
  - Filters notices by severity level. Accepted values: `info`, `warn`, `error`.
  - Example: :minlevel error

Combined Filter Example:
  :tabname "Part Studio 2" :minlevel error
```

--------------------------------

### Onshape FeatureScript Primitives

Source: https://cad.onshape.com/FsDoc/modeling

Primitives are basic operations for creating simple 3D solid bodies, such as fCuboid and fCylinder.

```FeatureScript
fCuboid(context, id, map)
fCylinder(context, id, map)
```

--------------------------------

### FeatureScript String Concatenation and toString

Source: https://cad.onshape.com/FsDoc/type-tags

Shows string concatenation using the '~' operator and the behavior of the `toString` function for library types in FeatureScript. It demonstrates how values are converted to strings during concatenation and for display.

```FeatureScript
2 * meter ~ "" // evaluates to a string like: "ValueWithUnits(27) : { \"unit\" : UnitSpec(28) : { \"meter\" : 1 } , \"value\" : 2 }"
tosTring(2 * meter) // returns "2 meter"
```

--------------------------------

### FeatureScript Type Conversion with 'as' Operator

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates type conversion using the 'as' operator in FeatureScript. It shows how type tags are applied and discarded, and how conversions work with enumerations and standard types. It also highlights that conversions to standard types do not retain type tags.

```FeatureScript
enum E { A }
E.A is E == true
E.A is string == true
(E.A as string) is string == true
(E.A as string) is E == false
```

```FeatureScript
x != (x as ValueWithUnits)
x == ((x as ValueWithUnits) as map)
```

```FeatureScript
(2 as Even) + (2 as Even) == 4
(2 as Even) + (2 as Even) != (4 as Even)
```

--------------------------------

### Specifying Parameter Icons

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates how to assign icons to parameters, either by referencing imported blob data or using predefined icons from the Icon enum.

```FeatureScript
annotation { "Name" : "My Length", "Icon" : IconNamespace::BLOB_DATA }
isLength(definition.myLength, LENGTH_BOUNDS);
```

```FeatureScript
export enum AxisEnum {
    annotation { "Name" : "Along X", "Icon" : Icon.ALONG_X }
    ALONG_X,
    annotation { "Name" : "Along Y", "Icon" : Icon.ALONG_Y }
    ALONG_Y,
    annotation { "Name" : "Along Z", "Icon" : Icon.ALONG_Z }
    ALONG_Z
}
...
annotation { "Name" : "Axis" }
definition.axis is AxisEnum;
```

--------------------------------

### FeatureScript Array Literals and Access

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates the creation of arrays using literals and accessing elements, including handling undefined values and out-of-bounds access.

```FeatureScript
var a = [1,2,3,4];
a[0] == 1;
var b = undefined;
var c = b?[0] // c will be undefined
var d = a?[1] // d == 2
```

--------------------------------

### Profiling Regeneration Timings in FeatureScript

Source: https://cad.onshape.com/FsDoc/debugging-in-feature-studios

Onshape allows profiling of Part Studios to determine which code segments (execution steps) contribute most to regeneration time. Hoverable markers display call counts and total time for steps exceeding 0.1ms.

```FeatureScript
// Example of code that might be profiled:
function complexCalculation(input: number): number {
  let result = 0;
  for (let i = 0; i < input; i++) {
    result += i * i;
  }
  return result;
}

// When this function is part of a Part Studio that is being profiled,
// the regeneration timings for the loop and the multiplication operation
// within the loop will be visible as hoverable markers.
```

--------------------------------

### Handle Multiple Operation Failures Gracefully

Source: https://cad.onshape.com/FsDoc/output

Shows how to process multiple operations within a loop, count failures, and report a warning if some operations fail, while failing the feature only if all operations fail.

```featurescript
var failedCount is number = 0;
for (var notch in notches)
{
    try
    {
        makeNotch(context, notch);
    }
    catch
    {
        setErrorEntities(context, id, { "entities" : notch.locationQuery });
        failedCount += 1;
    }
}
if (failedCount == size(notches))
    throw regenError("Failed to create notches");
if (failedCount > 0)
    reportFeatureWarning(context, id, "Failed to create " ~ failedCount ~ " notches.");
```

--------------------------------

### FeatureScript Calculations with Units (ValueWithUnits)

Source: https://cad.onshape.com/FsDoc/intro

Illustrates the use of the ValueWithUnits type in FeatureScript for defining and performing calculations with physical units, ensuring dimensional correctness.

```FeatureScript
const width is ValueWithUnits = 1.5 * inch;
const angle is ValueWithUnits = 30 * degree;
const area  is ValueWithUnits = 12 * centimeter^2;

```

--------------------------------

### FeatureScript Map Literals and Access

Source: https://cad.onshape.com/FsDoc/variables

Explains the declaration of map literals, including using variables as keys and accessing elements via dot notation or bracket notation. Covers safe navigation and handling of undefined values.

```FeatureScript
var map1 = {};
var map2 = { "a" : 1 };

var b = 2;
var map3 = {
    one : 1,
    (b) : "two",
    3.5 : "three point five"
};
// map3 is { "one" : 1, 2 : "two", 3.5 : "three point five" }

var val = map2.a;       // v is 1
val = map2["a"];        // v is 1

map2.a = 0;             // map2 is now { "a" : 0 }
map2.b = 1;             // map2 is now { "a" : 0, "b" : 1 }

var map4 = undefined;
var d = map4?.a;        // d is undefined

val = map2.c;           // val is undefined
map2.b = undefined      // map2 is now { "a" : 0 }
```

--------------------------------

### FeatureScript Table Cross-highlighting

Source: https://cad.onshape.com/FsDoc/tables

Explains how to enable cross-highlighting in FeatureScript tables by providing 'entities' to row and column constructors. This links table elements to geometry in the CAD model.

```FeatureScript
// Example of adding entities to a row for cross-highlighting
tableRow(columnIdToCell={...}, entities=[part1, sketch1])

// Example of adding entities to a column for cross-highlighting
tableColumnDefinition(id: "col1", name: "Column 1", entities=[edge1])
```

--------------------------------

### Print Text File Content with FeatureScript

Source: https://cad.onshape.com/FsDoc/imports

Demonstrates how to read and print the content of a text file using TextData. The text file must be under 100KB.

```typescript
annotation { "Feature Type Name" : "Text content" }
export const textFile = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Text data" }
        definition.message is TextData;
    }
    {
        var textContent = definition.message.textData;
        println(textContent);
    });

```

--------------------------------

### Place Part with Length Configuration

Source: https://cad.onshape.com/FsDoc/imports

Defines a feature to place a part with a specified length, overriding the default configuration.

```FeatureScript
annotation { "Feature Type Name" : "Place with length" }
export const placePartWithLength = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Part Studio", "ComputedConfigurationInputs" : [ "Length" ] }
        definition.partStudio is PartStudioData;
    }
    {
        const computedLength = getComputedLength(context, id, definition);
        const instantiator = newInstantiator(id + "instantiator");
        addInstance(instantiator, definition.partStudio, {
            "configurationOverride" : {
                "Length" : computedLength
            }
        });
        instantiate(context, instantiator);
    });

```

--------------------------------

### FeatureScript Vector Transformation and Trigonometric Comparison

Source: https://cad.onshape.com/FsDoc/intro

Demonstrates transforming a vector using rotation and comparing the result with a trigonometrically calculated point in FeatureScript, verifying geometric accuracy.

```FeatureScript
const start is Vector = vector(2, 0, 0) * inch;
const zAxis is Line   = line(vector(0, 0, 0) * inch, vector(0, 0, 1));
const end   is Vector = rotationAround(zAxis, 30 * degree) * start;

const goal  is Vector = vector(cos(30 * degree), sin(30 * degree), 0) * (2 * inch);

println(tolerantEquals(end, goal)); // Prints "true"

```

--------------------------------

### Onshape FeatureScript Sketch Creation

Source: https://cad.onshape.com/FsDoc/modeling

FeatureScript allows for the creation of sketches within custom features. Control over the sketch coordinate system is crucial, with functions like newSketchOnPlane and newSketch available. The skSolve call is necessary to generate sketch geometry.

```FeatureScript
newSketchOnPlane(context, plane)
newSketch(context)
skSolve(context, sketchId)
```

--------------------------------

### FeatureScript Box Declaration and Usage

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates how to declare, assign, and access values within FeatureScript boxes, including the use of the safe navigation operator.

```FeatureScript
var b1 = new box(1);
var b2 = b1;
var b3 = new box(b1);

var b = b1[];

b1[] = "new value";
println(b1[]);
println(b2[]);
println(b3[][]);

var b4 = undefined;
var b5 = b4?[];
```

--------------------------------

### FeatureScript Type Testing with 'is' Operator

Source: https://cad.onshape.com/FsDoc/type-tags

Illustrates type checking using the 'is' operator in FeatureScript. It shows how to verify if a value conforms to a specific type, including custom types and standard types, and how this interacts with type conversion.

```FeatureScript
x is map == true
(x is ValueWithUnits) == false
(x as ValueWithUnits) is map == true
(x as ValueWithUnits) is ValueWithUnits == true
```

--------------------------------

### FeatureScript Debug Function for Geometry

Source: https://cad.onshape.com/FsDoc/output

Illustrates how to use the `debug` function in FeatureScript to visualize entities and geometry within a Part Studio. It shows debugging queries for adjacent entities and geometric types like Lines and Vectors.

```FeatureScript
debug(context, qAdjacent(definition.edge1, AdjacencyType.EDGE, EntityType.FACE));
debug(context, qAdjacent(definition.edge2, AdjacencyType.VERTEX, EntityType.FACE));
```

```FeatureScript
var myLine is Line = evEdgeTangentLine(context, {
    "edge" : definition.edge1,
    "parameter" : 0.5
});
var myPoint is Vector = myLine.origin + myLine.direction * 2.5 * inch;

debug(context, myLine);
debug(context, myPoint);
```

--------------------------------

### FeatureScript Array Iteration

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates how to iterate over elements in an array using a standard for loop.

```FeatureScript
for (item in collection)
{
    println(item);
}
```

--------------------------------

### Create a Precisely Positioned Sketch Rectangle

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Demonstrates creating a sketch rectangle with coordinates dynamically calculated based on slot dimensions, ensuring accurate placement relative to the slot path.

```FeatureScript
skRectangle(sketch1, "rectangle1", {
        "firstCorner" :  vector(0 * inch,    definition.width / 2),
        "secondCorner" : vector(slotLength, -definition.width / 2)
});
```

--------------------------------

### FeatureScript Map Indexing and Assignment

Source: https://cad.onshape.com/FsDoc/type-tags

Illustrates how to access and assign values in FeatureScript maps using various key types, including quoted and unquoted strings, numbers, and undefined.

```FeatureScript
m["field"]
m[693.5]
m[undefined]
m.field
m.three = 3;
```

--------------------------------

### FeatureScript Safe Navigation Operators

Source: https://cad.onshape.com/FsDoc/type-tags

Demonstrates the use of safe navigation operators `?.` and `?[...]` in FeatureScript for accessing elements within containers like boxes, arrays, and maps. It highlights how these operators handle undefined values gracefully.

```FeatureScript
// Example usage of safe navigation operators would go here, 
// but the provided text only describes their functionality.
```

--------------------------------

### Full Feature Definition with UI Defaults and Filters

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Combines custom length bounds, parameter defaults, and refined filters within a Feature Studio definition.

```FeatureScript
export const SLOT_WIDTH_BOUNDS =
{
    (meter)      : [1e-5, 0.0025, 500],
    (centimeter) : 0.25,
    (millimeter) : 2.5,
    (inch)       : 0.1,
    (foot)       : 0.01,
    (yard)       : 0.0025
} as LengthBoundSpec;

export const BUMP_HEIGHT_BOUNDS =
{
    (meter)      : [1e-5, 0.00025, 500],
    (centimeter) : 0.025,
    (millimeter) : 0.25,
    (inch)       : 0.01,
    (foot)       : 0.001,
    (yard)       : 0.00025
} as LengthBoundSpec;

annotation { "Feature Type Name" : "Slot" }
export const slot = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Slot path", "Filter" : EntityType.EDGE && SketchObject.YES && GeometryType.LINE, "MaxNumberOfPicks" : 1 }
        definition.slotPath is Query;

        annotation { "Name" : "Part to cut", "Filter" : EntityType.BODY && BodyType.SOLID, "MaxNumberOfPicks" : 1 }
        definition.partToCut is Query;

        annotation { "Name" : "Width" }
        isLength(definition.width, SLOT_WIDTH_BOUNDS);

        annotation { "Name" : "Add bumps", "Default" : true }
        definition.addBumps is boolean;

        if (definition.addBumps)
        {
            annotation { "Name" : "Bump height" }
            isLength(definition.bumpHeight, BUMP_HEIGHT_BOUNDS);
        }
    }
...
```

--------------------------------

### Onshape FeatureScript Reference Parameter Annotation

Source: https://cad.onshape.com/FsDoc/uispec

Defines a reference parameter that can link to Part Studio tabs, allowing access to geometry or data. It specifies the expected item type and how the referenced data is stored.

```FeatureScript
annotation { "Name" : "Part Studio", "Filter" : PartStudioItemType.SOLID || PartStudioItemType.ENTIRE_PART_STUDIO }
definition.myPartStudio is PartStudioData;
```

--------------------------------

### FeatureScript Modeling Context and Bodies

Source: https://cad.onshape.com/FsDoc/modeling

Explains the concept of 'context' in FeatureScript, which stores modeling data like bodies, entities, variables, and feature error states. It details how geometry is grouped into independent bodies (solid, sheet, wire, point) and defines entities (faces, edges, vertices) as components of bodies. This is fundamental for any modeling operation in FeatureScript.

```FeatureScript
/*
Context: A builtin that stores modeling data, including bodies (solid, sheet, wire, and point), their constituent entities (faces, edges, and vertices), variables, feature error states, etc. All features, operations, and evaluation functions require a context to operate on.
See: https://cad.onshape.com/FsDoc/library.html#module-context.fs

Bodies: Geometry in a context is grouped into independent bodies. A body is comprised of zero or more vertices, faces, and edges and may represent a 3D (solid), 2D (sheet), 1D (wire), or 0D (point) region. A body is always connected.
Entities: Bodies, vertices, edges, and faces are collectively referred to as "entities". Every face, edge, and vertex belongs to a body.
*/

```

--------------------------------

### Place Imported CAD Geometry with FeatureScript

Source: https://cad.onshape.com/FsDoc/imports

Explains how to import foreign CAD data into a Part Studio using the opImportForeign function. It requires CADImportData and checks if the data is not empty.

```typescript
annotation { "Feature Type Name" : "Place import" }
export const placeBrep = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Brep" }
        definition.brep is CADImportData;
    }
    {
        if (size(definition.brep) > 0)
        {
            opImportForeign(context, id + "importForeign1", {
                "blobData" : definition.brep
            });
        }
    });

```

--------------------------------

### FeatureScript Equality and Inequality Operators

Source: https://cad.onshape.com/FsDoc/relational

Demonstrates the use of `==` and `!=` operators for comparing FeatureScript values. These operators check for exact equality based on type, type tag, and value.

```FeatureScript
value1 == value2
value1 != value2
```

--------------------------------

### FeatureScript Throw Statement

Source: https://cad.onshape.com/FsDoc/exceptions

Illustrates how to explicitly raise exceptions using the 'throw' statement in FeatureScript. Shows throwing without a value, with a string, with a map, and rethrowing a caught exception.

```FeatureScript
throw;
throw "an error has occurred";
throw { message : ErrorStringEnum.TOO_MANY_ENTITIES_SELECTED };
try { f(); } catch (e) { throw e; }
```

--------------------------------

### FeatureScript Enum Declaration and Usage

Source: https://cad.onshape.com/FsDoc/type-tags

Shows how to declare enumerations using the `enum` keyword and how to use them as types and values, including tagging strings with enumeration types.

```FeatureScript
enum Example { A, B }

Example is map; // It's a value!
Example.A is Example; // It's a type!
Example.A is string;

"A" as Example // same as Example.A
// "Z" as Example // runtime error
// 0 as Example // runtime error
```

--------------------------------

### FeatureScript Lambda Function Declaration

Source: https://cad.onshape.com/FsDoc/variables

Illustrates various ways to declare lambda functions in FeatureScript using both traditional function syntax and the concise arrow (=>) syntax.

```FeatureScript
var f = function(x) { return x + 1; };
var f = x => x + 1;

const twice = (v is ValueWithUnits) returns ValueWithUnits => v * 2;
const thrice = (v is ValueWithUnits) => { return v * 3; };
var fourfold = v => v * 4;
var apply = (f is function, val) => f(val);
var doubled = (val is ValueWithUnits) => apply(twice, val);
var cascaded = (a => b => c => a * b + c)(4)(5)(6);
```

--------------------------------

### Debug Coordinate System

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Prints the details of a Coordinate System, which includes its origin and axis directions. This is helpful for verifying the coordinate system's definition.

```FeatureScript
debug(context, cSys);
```

--------------------------------

### Block Statement

Source: https://cad.onshape.com/FsDoc/syntax

A sequence of statements enclosed in curly braces, executed in order.

```FeatureScript
{
    var x = 0;
    f(x);
}
```

--------------------------------

### Create Sketch on a Defined Plane

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Shows how to create a sketch specifically on a pre-defined sketch plane, which is a common requirement for precise sketch placement.

```FeatureScript
var sketch1 = newSketch(context, id + "sketch1", {
        "sketchPlane" : sketchPlane
});
```

--------------------------------

### FeatureScript Custom Table Definition

Source: https://cad.onshape.com/FsDoc/tables

Defines a custom table in FeatureScript to display part volumes. It iterates through all modifiable solid bodies, retrieves their names and volumes, and formats them into a table. This demonstrates the use of `defineTable`, `tableColumnDefinition`, `evaluateQuery`, `getProperty`, `evVolume`, `table`, and `tableRow`.

```FeatureScript
annotation { "Table Type Name" : "Part volumes" }
export const partVolumes = defineTable(function(context is Context, definition is map) returns Table
    precondition
    {
    }
    {
        var columns = [tableColumnDefinition("name", "Part name"),
                       tableColumnDefinition("volume", "Volume")];

        var rows = [];
        for (var part in evaluateQuery(context, qAllModifiableSolidBodies()))
        {
            var partName = getProperty(context, { "entity" : part, "propertyType" : PropertyType.NAME } );
            var volume = evVolume(context, { "entities" : part });
            rows = append(rows, tableRow({ "name" : partName, "volume" : volume }));
        }

        return table("Part volumes", columns, rows);
    });

```

--------------------------------

### Place Solid Bodies Feature

Source: https://cad.onshape.com/FsDoc/imports

Defines a feature to place only solid bodies from a Part Studio, filtering out other item types.

```FeatureScript
annotation { "Feature Type Name" : "Place solids" }
export const myFeature = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Parts", "Filter" : PartStudioItemType.SOLID || PartStudioItemType.ENTIRE_PART_STUDIO }
        definition.parts is PartStudioData;
    }
    {
        definition.parts.partQuery = qBodyType(definition.things.partQuery, BodyType.SOLID);

        const instantiator = newInstantiator(id + "instantiator");
        addInstance(instantiator, definition.parts);
        instantiate(context, instantiator);
    });

```

--------------------------------

### FeatureScript Querying for Topological Entities

Source: https://cad.onshape.com/FsDoc/modeling

Details the use of 'Queries' in FeatureScript to refer to topological entities (vertices, edges, faces, and bodies). Queries act as criteria for selecting geometry, such as entities created by a specific operation or those containing a point. Queries are robust against changes in the feature list and can be combined. The `evaluateQuery` function is used to find matching entities.

```FeatureScript
/*
Queries: Used to refer to topological entities (vertices, edges, faces, and bodies) in a context. A Query encodes criteria that geometry must satisfy.
Examples:
- qCreatedBy(operationId): Specifies geometry created during a certain operation.
- qContainsPoint(vector): Specifies geometry that touches a specified point.

Evaluation:
- evaluateQuery(context, query): Returns an array of transient queries – one for each entity in the context matching the query.

See: https://cad.onshape.com/FsDoc/library.html#module-query.fs
*/

```

--------------------------------

### FeatureScript Type Checking

Source: https://cad.onshape.com/FsDoc/debugging-in-feature-studios

FeatureScript performs type checking on variables with type tags during execution. Failures are reported as warnings, and type checks only run on monitored Part Studios to improve performance.

```FeatureScript
function checkType(value: number): void {
  // Type check for 'number' is implicitly handled by FeatureScript
  // If 'value' is not a number, a warning will be generated during monitored execution.
}
```

--------------------------------

### Cadence Compound Assignment Operators

Source: https://cad.onshape.com/FsDoc/type-tags

Lists and explains the behavior of compound assignment operators in Cadence, which combine an operation with assignment, ensuring side effects on the left-hand side occur only once.

```cadence
x += y
x -= y
x *= y
x /= y
x ^= y
x %= y
x ||= y
x &&= y
x ??= y
x ~= y

// x op= y is equivalent to x = x op y, but side effects on x only happen once.
```

--------------------------------

### Full FeatureScript Code for Sketch Slot Bumps

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Provides the complete FeatureScript code for creating a sketch with slot bumps. This includes initializing the sketch, drawing a rectangle, conditionally adding bumps with arcs, and solving the sketch.

```FeatureScript
var sketch1 = newSketchOnPlane(context, id + "sketch1", {
        "sketchPlane" : sketchPlane
});

var slotLength is ValueWithUnits = norm(endPosition - startPosition);
skRectangle(sketch1, "rectangle1", {
        "firstCorner" :  vector(0 * inch,    definition.width / 2),
        "secondCorner" : vector(slotLength, -definition.width / 2)
});

if (definition.addBumps)
{
    var bumpDistance = slotLength / 10;          // Distance from end of slot to start of bump
    var bumpWidth    = slotLength / 5;
    var bumpHeight   = definition.bumpHeight;

    for (var side in [-1, 1])
    {
        skArc(sketch1, "arc_0_" ~ side, {
                "start" : vector(bumpDistance, (definition.width / 2) * side),
                "mid" : vector(bumpDistance + bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
                "end" : vector(bumpDistance + bumpWidth, (definition.width / 2) * side)
        });

        skArc(sketch1, "arc_1_" ~ side, {
                "start" : vector(slotLength - bumpDistance, (definition.width / 2) * side),
                "mid" : vector(slotLength - bumpDistance - bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
                "end" : vector(slotLength - bumpDistance - bumpWidth, (definition.width / 2) * side)
        });
    }
}
skSolve(sketch1);
```

--------------------------------

### FeatureScript Operator Overload Declaration

Source: https://cad.onshape.com/FsDoc/top-level

Shows how to declare an operator overload in FeatureScript, specifying the operator, argument types, and return type.

```FeatureScript
operator*(x is Vector, y is number)
{
    ...
}
```

--------------------------------

### Feature UI Annotations

Source: https://cad.onshape.com/FsDoc/uispec

Details the optional annotations that can be added to a FeatureScript constant to customize its behavior and appearance in the Onshape UI. These include manipulator functions, editing logic, filtering, naming, tooltips, UI hints, descriptions, and icons.

```FeatureScript
annotation {
  "Feature Type Name" : "Fancy Fillet",
  "Manipulator Change Function" : "myManipulatorChangeFunction",
  "Editing Logic Function" : "myEditingLogicFunction",
  "Filter Selector" : ["foo", "bar"],
  "Feature Name Template" : "Fancy Fillet ${id}",
  "Tooltip Template" : "Creates a fancy fillet",
  "UIHint" : "UIHint.NO_PREVIEW_PROVIDED",
  "Feature Type Description" : "A description of the feature.",
  "Icon" : "path/to/icon.svg",
  "Description Image" : "path/to/description.png"
}

```

--------------------------------

### FeatureScript Parameter Group Definition

Source: https://cad.onshape.com/FsDoc/uispec

Demonstrates how to define a collapsible parameter group in FeatureScript using an annotation with 'Group Name'. It shows how to include multiple parameters within a group and how to control the default collapsed state and driving parameter.

```FeatureScript
annotation { "Group Name" : "My Group" }
{
    annotation { "Name" : "My Boolean" }
    definition.myBoolean is boolean;

    annotation { "Name" : "My Length" }
    isLength(definition.myLength, LENGTH_BOUNDS);
}
```

--------------------------------

### Create 3D Spline from CSV with FeatureScript

Source: https://cad.onshape.com/FsDoc/imports

Shows how to create a 3D spline from CSV data using the opFitSpline function. It takes TableData containing spline points as input.

```typescript
annotation { "Feature Type Name" : "Create 3D spline" }
export const create3dSpline = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Spline points" }
        definition.splinePointsTable is TableData;
    }
    {
        const csvUnits = inch;
        const points = mapArray(definition.splinePointsTable.csvData, function(row) {
            // map CSV columns 0, 1, and 2 to a vector of X, Y, Z
            return vector(row[0], row[1], row[2]) * csvUnits;
        });

        opFitSpline(context, id + "fitSpline1", {
                "points" : points
        });
    });

```

--------------------------------

### String Parameter Specification

Source: https://cad.onshape.com/FsDoc/uispec

Defines a string parameter with a user-facing name and an optional default value. String parameters are rendered as text boxes.

```FeatureScript
annotation { "Name" : "My String", "Default" : "My default value" }
definition.myString is string;
```

--------------------------------

### Sketch Text from JSON with FeatureScript

Source: https://cad.onshape.com/FsDoc/imports

Illustrates how to sketch text using JSON data with the skText function. It requires JSONData containing person information, specifically the first name.

```typescript
annotation { "Feature Type Name" : "Sketch text" }
export const sketchText = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Person data" }
        definition.personData is JSONData;
    }
    {
        var sketch1 = newSketch(context, id + "sketch1", {
                "sketchPlane" : qCreatedBy(makeId("Top"), EntityType.FACE)
        });
        skText(sketch1, "text1", {
                "text" : definition.personData.jsonData.firstName,
                "fontName" : "OpenSans-Regular.ttf",
                "firstCorner" : vector(0, 0) * inch,
                "secondCorner" : vector(1, 1) * inch
        });
        skSolve(sketch1);
    });

```

--------------------------------

### Length Quantity Parameter Specification

Source: https://cad.onshape.com/FsDoc/uispec

Defines a length quantity parameter with a user-facing name. Quantities are rendered as text boxes accepting numbers, expressions, or numbers with units.

```FeatureScript
annotation { "Name" : "My Length" }
isLength(definition.myLength, LENGTH_BOUNDS);
```

--------------------------------

### Handle Fillet Operation Errors

Source: https://cad.onshape.com/FsDoc/output

Demonstrates how to catch a regeneration error from an `opFillet` operation and re-throw a more informative error, highlighting the problematic geometry.

```featurescript
try
{
    opFillet(context, id + "fillet", { "entities" : ribPartEdges, "radius" : definition.filletRadius });
}
catch
{
    throw regenError("Failed to fillet the interface between the rib and the selected part.", ribPartEdges);
}
```

--------------------------------

### Debugging Queries in Onshape FeatureScript

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

This snippet shows how to use the `debug` function in Onshape FeatureScript to visualize entities that match a given query. This is useful for verifying that queries are selecting the correct geometry. The comment indicates how to disable the debug call.

```featurescript
debug(context, endPoints);

// debug(context, endPoints);
```

--------------------------------

### Onshape FeatureScript Operations

Source: https://cad.onshape.com/FsDoc/modeling

Operations are standard library functions for creating or modifying geometry. They begin with 'op' and take a Context, an operation Id, and a definition map. Operations modify the context and their changes are tracked by a unique Id.

```FeatureScript
opFillet(context, id, definitionMap)
opExtrude(context, id, definitionMap)
opDraft(context, id, definitionMap)
opBoolean(context, id, definitionMap)
opPoint(context, id, definitionMap)
opThicken(context, id, definitionMap)
```

--------------------------------

### FeatureScript Variable Declaration for Bumps

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Shows how to declare and initialize variables within an if block to calculate dimensions and positions for sketch slot bumps. These variables are local to the block.

```FeatureScript
var bumpDistance   = slotLength / 10;          // Distance from end of slot to start of bump
var bumpWidth      = slotLength / 5;
var bumpHeight   = definition.bumpHeight;
```

--------------------------------

### Use Custom Length Bounds for Parameters

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Applies custom defined length bounds to length parameters in the Feature Studio dialog.

```FeatureScript
annotation { "Name" : "Width" }
isLength(definition.width, SLOT_WIDTH_BOUNDS);
```

```FeatureScript
annotation { "Name" : "Bump height" }
isLength(definition.bumpHeight, BUMP_HEIGHT_BOUNDS);
```

--------------------------------

### Enum Definition and Parameter Specification

Source: https://cad.onshape.com/FsDoc/uispec

Defines an exported enum with user-visible names for its values and specifies an enum parameter using the enum. Enum parameters are rendered as dropdown menus.

```FeatureScript
export enum MyOption
{
    annotation { "Name" : "Option One" }
    ONE,
    annotation { "Name" : "Option Two" }
    TWO
}
...
annotation { "Name" : "My Enum" }
definition.myEnum is MyOption;
```

--------------------------------

### Specifying Custom Feature Icons

Source: https://cad.onshape.com/FsDoc/uispec

Explains how to set a custom icon for a feature by referencing an imported SVG blob data.

```FeatureScript
IconNamespace::import(...);

annotation { "Feature Type Name" : "MyFeature", "Icon" : IconNamespace::BLOB_DATA }
export const myFeature = defineFeature(function(context is Context, id is Id, definition is map)
...
```

--------------------------------

### While Loop

Source: https://cad.onshape.com/FsDoc/syntax

Executes a statement repeatedly as long as a boolean condition remains true.

```FeatureScript
while (!done)
{
    x += 1;
    done = f(x);
}
```

--------------------------------

### FeatureScript Map Operations with Undefined Values

Source: https://cad.onshape.com/FsDoc/type-tags

Explains the behavior of arithmetic operations on map keys that may hold undefined values, highlighting potential errors and the effect of storing undefined.

```FeatureScript
var x = {}; // empty map
x.k += 1; // same as x.k = x.k + 1; which is an error
```

--------------------------------

### Debug Vector Position

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Prints the exact location of a Vector in meters and highlights the point in the Part Studio. This aids in debugging geometric calculations.

```FeatureScript
debug(context, startPosition);
```

--------------------------------

### FeatureScript Enum Declaration

Source: https://cad.onshape.com/FsDoc/variables

Shows the syntax for declaring an enumeration (enum) in FeatureScript, defining a finite set of named constants.

```FeatureScript
export enum LumberSize
{
    TWO_BY_FOUR,
    TWO_BY_SIX,
    TWO_BY_EIGHT
}
```

--------------------------------

### Iterating with FeatureScript For Loop

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Illustrates the use of a for loop to iterate over an array of values ([-1, 1]) to apply operations to both sides of the slot. This reduces code duplication.

```FeatureScript
for (var side in [-1, 1])
{

}
```

--------------------------------

### For Loop

Source: https://cad.onshape.com/FsDoc/syntax

Executes a statement iteratively based on initialization, condition, and increment expressions. The increment must be a function call or assignment.

```FeatureScript
for (var i = 0; i != 10; i += 1)
    f(i);
```

--------------------------------

### Sketch Profile Selection

Source: https://cad.onshape.com/FsDoc/imports

Specifies a reference parameter for selecting a single sketch profile, using a filter and limiting the number of picks.

```FeatureScript
annotation { "Name" : "Sketch profile", "Filter" : PartStudioItemType.SKETCH, "MaxNumberOfPicks" : 1 }
definition.sketchProfile is PartStudioData;

```

--------------------------------

### Onshape FeatureScript Feature Patterns

Source: https://cad.onshape.com/FsDoc/modeling

To correctly apply pattern transforms in custom features, use the getRemainderPatternTransform function. This function helps determine the appropriate transform for feature patterns.

```FeatureScript
getRemainderPatternTransform(context, map)
```

--------------------------------

### Define Custom Length Bounds

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Defines custom bounds for length parameters using LengthBoundSpec. This allows for precise control over default values and units.

```FeatureScript
FeatureScript 1890;
import(path : "onshape/std/common.fs", version : "1890.0");

export const SLOT_WIDTH_BOUNDS =
{
    (meter)      : [1e-5, 0.0025, 500],
    (centimeter) : 0.25,
    (millimeter) : 2.5,
    (inch)       : 0.1,
    (foot)       : 0.01,
    (yard)       : 0.0025
} as LengthBoundSpec;

export const BUMP_HEIGHT_BOUNDS =
{
    (meter)      : [1e-5, 0.00025, 500],
    (centimeter) : 0.025,
    (millimeter) : 0.25,
    (inch)       : 0.01,
    (foot)       : 0.001,
    (yard)       : 0.00025
} as LengthBoundSpec;
```

--------------------------------

### Querying Line Endpoints in Onshape FeatureScript

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

This FeatureScript code uses the `qAdjacent` function to query for vertices that are topologically adjacent to a given line path. The result is stored as a `Query` variable, which can then be used for further operations or debugging.

```featurescript
var endPoints is Query = qAdjacent(definition.slotPath, AdjacencyType.VERTEX, EntityType.VERTEX);
```

--------------------------------

### Create a Sketch Rectangle

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Illustrates how to add a rectangle entity to a sketch using the `skRectangle` function with specified corner coordinates.

```FeatureScript
skRectangle(sketch1, "rectangle1", {
        "firstCorner" : vector(0, 0) * inch,
        "secondCorner" : vector(1, 1) * inch
});
```

--------------------------------

### FeatureScript Slot Feature Definition

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Defines the 'Slot' feature in FeatureScript, including preconditions, parameters, and the operational logic for creating the slot with optional bumps. It utilizes Onshape's API for geometric operations like sketching, extruding, and boolean operations.

```FeatureScript
FeatureScript 1890;
import(path : "onshape/std/common.fs", version : "1890.0");

export const SLOT_WIDTH_BOUNDS =
{
    (meter)      : [1e-5, 0.0025, 500],
    (centimeter) : 0.25,
    (millimeter) : 2.5,
    (inch)       : 0.1,
    (foot)       : 0.01,
    (yard)       : 0.0025
} as LengthBoundSpec;

export const BUMP_HEIGHT_BOUNDS =
{
    (meter)      : [1e-5, 0.00025, 500],
    (centimeter) : 0.025,
    (millimeter) : 0.25,
    (inch)       : 0.01,
    (foot)       : 0.001,
    (yard)       : 0.00025
} as LengthBoundSpec;

annotation { "Feature Type Name" : "Slot" }
export const slot = defineFeature(function(context is Context, id is Id, definition is map)
    precondition
    {
        annotation { "Name" : "Slot path", "Filter" : EntityType.EDGE && SketchObject.YES && GeometryType.LINE, "MaxNumberOfPicks" : 1 }
        definition.slotPath is Query;

        annotation { "Name" : "Part to cut", "Filter" : EntityType.BODY && BodyType.SOLID, "MaxNumberOfPicks" : 1 }
        definition.partToCut is Query;

        annotation { "Name" : "Width" }
        isLength(definition.width, SLOT_WIDTH_BOUNDS);

        annotation { "Name" : "Add bumps", "Default" : true }
        definition.addBumps is boolean;

        if (definition.addBumps)
        {
            annotation { "Name" : "Bump height" }
            isLength(definition.bumpHeight, BUMP_HEIGHT_BOUNDS);
        }
    }
    {
        var endPoints is Query = qAdjacent(definition.slotPath, AdjacencyType.VERTEX, EntityType.VERTEX);
        // debug(context, endPoints);

        var startPosition is Vector = evVertexPoint(context, {
                "vertex" : qNthElement(endPoints, 0)
        });
        var endPosition is Vector = evVertexPoint(context, {
                "vertex" : qNthElement(endPoints, 1)
        });
        // debug(context, startPosition);

        var xDirection is Vector = normalize(endPosition - startPosition);
        var zDirection is Vector = evOwnerSketchPlane(context, {
                "entity" : definition.slotPath
        }).normal;

        var cSys is CoordSystem = coordSystem(startPosition, xDirection, zDirection);
        // debug(context, cSys);

        var sketchPlane is Plane = plane(cSys);

        var sketch1 = newSketchOnPlane(context, id + "sketch1", {
                "sketchPlane" : sketchPlane
        });

        var slotLength is ValueWithUnits = norm(endPosition - startPosition);
        skRectangle(sketch1, "rectangle1", {
                "firstCorner" :  vector(0 * inch,    definition.width / 2),
                "secondCorner" : vector(slotLength, -definition.width / 2)
        });

        if (definition.addBumps)
        {
            var bumpDistance = slotLength / 10;          // Distance from end of slot to start of bump
            var bumpWidth    = slotLength / 5;
            var bumpHeight   = definition.bumpHeight;

            for (var side in [-1, 1])
            {
                skArc(sketch1, "arc_0_" ~ side, {
                        "start" : vector(bumpDistance, (definition.width / 2) * side),
                        "mid" : vector(bumpDistance + bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
                        "end" : vector(bumpDistance + bumpWidth, (definition.width / 2) * side)
                });

                skArc(sketch1, "arc_1_" ~ side, {
                        "start" : vector(slotLength - bumpDistance, (definition.width / 2) * side),
                        "mid" : vector(slotLength - bumpDistance - bumpWidth / 2, (definition.width / 2 - bumpHeight) * side),
                        "end" : vector(slotLength - bumpDistance - bumpWidth, (definition.width / 2) * side)
                });
            }
        }
        skSolve(sketch1);

        var regionToExtrude = qContainsPoint(qSketchRegion(id + "sketch1"), cSys.origin);
        debug(context, regionToExtrude);

        opExtrude(context, id + "extrude1", {
              "entities" : regionToExtrude,
              "direction" : zDirection,
              "endBound" : BoundingType.THROUGH_ALL,
              "startBound" : BoundingType.THROUGH_ALL
        });

        opBoolean(context, id + "boolean1", {
              "tools" : qCreatedBy(id + "extrude1", EntityType.BODY),
              "targets" : definition.partToCut,
              "operationType" : BooleanOperationType.SUBTRACTION
        });

        opDeleteBodies(context, id + "delete1", {
              "entities" : qCreatedBy(id + "sketch1", EntityType.BODY)
        });
    });

```

--------------------------------

### Create Sketch Plane

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Creates a Plane from a Coordinate System. A sketch plane defines the origin and orientation for placing sketch entities accurately.

```FeatureScript
var sketchPlane is Plane = plane(cSys);
```

--------------------------------

### FeatureScript Conditional Parameter Group

Source: https://cad.onshape.com/FsDoc/uispec

Illustrates creating a parameter group that is conditionally displayed based on a boolean parameter. It shows how to set the group to be not collapsed by default and link its visibility to a 'Driving Parameter'.

```FeatureScript
annotation { "Name" : "Additional Options" }
definition.additionalOptions is boolean;

if (definition.additionalOptions)
{
    annotation { "Group Name" : "Options", "Collapsed By Default" : false, "Driving Parameter" : "additionalOptions" }
    {
        annotation { "Name" : "Option" }
        definition.option is string;
    }
}
```

--------------------------------

### Onshape FeatureScript Extrude, Thicken, Delete, and Boolean Operations

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

This snippet demonstrates a sequence of Onshape FeatureScript operations: extruding a path, thickening the extruded body, deleting the original extrusion, and performing a boolean subtraction. It's used to create a slot feature.

```featurescript
    opExtrude(context, id + "extrude1", {
           "entities" : definition.slotPath,
           "direction" : evOwnerSketchPlane(context, {"entity" : definition.slotPath}).normal,
           "endBound" : BoundingType.THROUGH_ALL,
           "startBound" : BoundingType.THROUGH_ALL
    });

    opThicken(context, id + "thicken1", {
           "entities" : qCreatedBy(id + "extrude1", EntityType.BODY),
           "thickness1" : definition.width / 2,
           "thickness2" : definition.width / 2
    });

    opDeleteBodies(context, id + "delete1", {
           "entities" : qCreatedBy(id + "extrude1", EntityType.BODY)
    });

    opBoolean(context, id + "boolean1", {
           "tools" : qCreatedBy(id + "thicken1", EntityType.BODY),
           "targets" : definition.partToCut,
           "operationType" : BooleanOperationType.SUBTRACTION
    });

});
```

--------------------------------

### FeatureScript Type Constraints on Variables

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates how to enforce type constraints on variables using the `is` keyword. Assigning a value of an incompatible type will result in a runtime error.

```FeatureScript
var x = 1;
var y is number = 1;

x = "string";           // succeeds
y = "string";           // runtime error: type mismatch

var nil is number = 0;  // succeeds
const n is number = 10; // succeeds

var nil3 is number;                 // parse error: variable with type must be initialized
var nil2 is number = undefined;     // runtime error: value assigned to variable should be number, was undefined
```

--------------------------------

### Custom Type Declaration with Predicate

Source: https://cad.onshape.com/FsDoc/top-level

Shows how to declare and export a custom type 'Person' using a typecheck predicate 'canBePerson' to define validation rules for the type.

```featurescript
export type Person typecheck canBePerson;

export predicate canBePerson(value)
{
    value is map;
    value.firstName is string;
    value.age is number;
    value.age > 0;
}
```

--------------------------------

### Onshape FeatureScript Geometric Types and Comparison

Source: https://cad.onshape.com/FsDoc/modeling

FeatureScript provides geometric types like Vector, Line, Plane, and CoordSystem for geometric computations. For comparing geometric objects, use the tolerantEquals method to handle floating-point round-off errors.

```FeatureScript
Vector
Line
Plane
CoordSystem
tolerantEquals(object1, object2)
```

--------------------------------

### Return Statement

Source: https://cad.onshape.com/FsDoc/syntax

Exits the current function, optionally returning a value. The return type must match the declared function return type.

```FeatureScript
return; // = return undefined
return 0;
```

--------------------------------

### Evaluate Vertex Positions

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Retrieves the 3D positions of two endpoints using evVertexPoint and qNthElement. This is useful for defining geometric references within a Part Studio.

```FeatureScript
var startPosition is Vector = evVertexPoint(context, {
        "vertex" : qNthElement(endPoints, 0)
});
var endPosition is Vector = evVertexPoint(context, {
        "vertex" : qNthElement(endPoints, 1)
});
```

--------------------------------

### Define Computed Part Property Function

Source: https://cad.onshape.com/FsDoc/computed-part-properties

Defines a computed part property function in FeatureScript that calculates the volume of a part. It requires the 'onshape/std/common.fs' import and uses specific annotations and functions to identify and define the property.

```FeatureScript
annotation { "Property Function Name" : "computeVolume" }
export const computeVolume = defineComputedPartProperty(
             function(context is Context, part is Query, definition is map) returns ValueWithUnits
    {
        return evVolume(context, { "entities" : part });
    });

```

--------------------------------

### Constant Declaration

Source: https://cad.onshape.com/FsDoc/syntax

Declares a constant with a required initializer. Constants cannot be modified after initialization.

```FeatureScript
const one = 1;
const e is number = exp(1);
```

--------------------------------

### FeatureScript Value Semantics

Source: https://cad.onshape.com/FsDoc/variables

Illustrates FeatureScript's value semantics, where assignments create copies of values rather than sharing references. This behavior applies to function arguments and return values as well.

```FeatureScript
var a = 10;
var b = a;
a = 20;

println(a);     // prints "20"
println(b);     // prints "10"
```

--------------------------------

### FeatureScript Table Cell Content Types

Source: https://cad.onshape.com/FsDoc/tables

Details the various types of content that can be displayed within a FeatureScript table cell, including plain strings, values with units, template strings for formatted text, and error indicators.

```FeatureScript
// String
"Simple Text"

// ValueWithUnits
valueWithUnits(1.5, "m") // Displays as "1.5 m" in a metric workspace

// TemplateString
templateString("Length: #l", { "l": valueWithUnits(0.5, "m") }) // Displays as "Length: 0.5 m"

// TableCellError
tableCellError("Error", "Details about the error")
```

--------------------------------

### FeatureScript Less Than Operator

Source: https://cad.onshape.com/FsDoc/relational

Shows the usage of the `<` operator for comparing FeatureScript values. This operator can be used for standard types and can also be overloaded for custom types with type tags.

```FeatureScript
value1 < value2
```

--------------------------------

### FeatureScript Enum Comparison

Source: https://cad.onshape.com/FsDoc/relational

Demonstrates how enums can be compared using relational operators (`<`, `>`, etc.) as long as both operands belong to the same enum type. Comparing enums from different types will result in an error.

```FeatureScript
// Assuming EnumA and EnumB are defined enums
EnumA.X < EnumA.Y  // Evaluates to true if X precedes Y in EnumA definition
// EnumA.X < EnumB.Y  // This will throw an error
```

--------------------------------

### FeatureScript Try Silent Modifier

Source: https://cad.onshape.com/FsDoc/exceptions

Explains the 'try silent' modifier in FeatureScript for suppressing exception reporting. Demonstrates its use with both expression and statement forms.

```FeatureScript
var x = try silent (myMap.submapThatMayNotExist.subMapKey);
```

```FeatureScript
try silent { plane = evPlane(context, { face : mightBePlane }); }
```

--------------------------------

### FeatureScript Type Tag Check

Source: https://cad.onshape.com/FsDoc/variables

Illustrates how to use the 'is' operator to check if a variable conforms to a specific type tag.

```FeatureScript
if (x is Vector)
{
    // do something
}
```

--------------------------------

### FeatureScript Statement Blocks and Scopes

Source: https://cad.onshape.com/FsDoc/variables

Explains the use of statement blocks delimited by braces `{}` in FeatureScript for control flow and defining variable scopes. Variables declared within a block are not accessible outside of it.

```FeatureScript
var visible = "hello";
if (condition)
{
    var invisible = "hello";
}
println(visible);       // prints "hello"
println(invisible);     // semantic error: variable invisible not found
```

--------------------------------

### Custom Length Quantity Parameter Specification with Default

Source: https://cad.onshape.com/FsDoc/uispec

Defines a length quantity parameter with a user-facing name and a custom default value of 1.75 inches.

```FeatureScript
annotation { "Name" : "My Length" }
isLength(definition.myLength, { (inch) : [ 0, 1.75, 10000 ] } as LengthBoundSpec);
```

--------------------------------

### Onshape Editing Logic Function Signature

Source: https://cad.onshape.com/FsDoc/uispec

Defines the signature of an Onshape editing logic function, outlining its arguments and return type. The function receives context, feature ID, old and new definitions, creation status, specified parameters, and hidden bodies.

```typescript
export function onFeatureChange(context is Context, id is Id, oldDefinition is map, definition is map,
                                isCreating is boolean, specifiedParameters is map, hiddenBodies is Query) returns map
{
    // modify the definition
    ...

    return definition;
}
```

--------------------------------

### For-In Loop

Source: https://cad.onshape.com/FsDoc/syntax

Iterates over elements of an array or map. Can use one variable (value for arrays, {key, value} for maps) or two (index/key and value).

```FeatureScript
// one-variable iteration over an array
for (var x in [1, 2, 3])
    println(x); // Prints: 1, 2, 3

// one-variable iteration over a map
for (var x in { "a": 1, "b": 2, "c": 3 })
{
    var key = x.key;
    var value = x.value;
    println(key ~ "=" ~ value); // Prints: a=1, b=2, c=3
}

// two-variables, map
for (var key, value in { a: 1, b: 2, c: 3 })
    println(key ~ "=" ~ value)  // Prints: a=1, b=2, c=3

// two-variables, array
for (var index, value in [100, 200, 300])
    println(index ~ "=" ~ value)  // Prints: 0=100, 1=200, 2=300
```

--------------------------------

### Boolean Parameter Specification

Source: https://cad.onshape.com/FsDoc/uispec

Defines a boolean parameter with a user-facing name and an optional default value. Boolean parameters are rendered as checkboxes.

```FeatureScript
annotation { "Name" : "My Boolean", "Default" : true }
definition.myBoolean is boolean;
```

--------------------------------

### Expression Statement

Source: https://cad.onshape.com/FsDoc/syntax

An expression followed by a semicolon. Typically used for function calls that perform an action.

```FeatureScript
x;   // valid reference to variable x, but does nothing
f(); // calls a function that might do something
```

--------------------------------

### Break and Continue Statements

Source: https://cad.onshape.com/FsDoc/syntax

Break exits the innermost loop. Continue exits the current loop iteration, proceeding to the next (or increment in a for loop).

```FeatureScript
break; // Exits loop
continue; // Skips current iteration
```

--------------------------------

### Conditional Bumps in FeatureScript

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Demonstrates how to use an if statement to conditionally add bumps to a sketch based on the 'definition.addBumps' boolean parameter. This is a fundamental control flow structure in FeatureScript.

```FeatureScript
if (definition.addBumps)
{

}
```

--------------------------------

### Add boolean parameter 'addBumps'

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Adds a boolean parameter named 'addBumps' to the feature definition, allowing users to control the addition of bumps to the slot.

```FeatureScript
annotation { "Name" : "Add bumps" }
definition.addBumps is boolean;
```

--------------------------------

### FeatureScript Number Type and Literals

Source: https://cad.onshape.com/FsDoc/variables

Describes the `number` type in FeatureScript as a 64-bit floating-point number. It covers various literal formats, including integers, decimals, and scientific notation. Operations that would result in NaN in other languages throw an error.

```FeatureScript
var one = 1;
var alsoOne = 1.0;
var bitLess = -.01;
var million = 1e6;
var infinity = inf;
```

--------------------------------

### If Statement

Source: https://cad.onshape.com/FsDoc/syntax

Executes a statement (usually a block) based on a boolean condition. Supports an optional else clause.

```FeatureScript
if (condition)
    statement;
else
    else_statement;
```

--------------------------------

### Onshape Editing Logic Function

Source: https://cad.onshape.com/FsDoc/uispec

Explains the role of the editing logic function in Onshape custom features. It's called when a feature definition changes, enabling intelligent default parameter setting and other logic.

```javascript
// Example usage within a feature definition:
// editingLogic: function(context, definition, userEdits) { ... }
```

--------------------------------

### Variable Declaration

Source: https://cad.onshape.com/FsDoc/syntax

Declares a variable with an optional type restriction and initializer. If a type is specified, an initializer is required.

```FeatureScript
var x;
var y = 0;
var z is Vector = vector(0, 0);
```

--------------------------------

### FeatureScript Enum Value Assignment

Source: https://cad.onshape.com/FsDoc/variables

Demonstrates how to assign a value from a declared enum to a variable, including the type tag.

```FeatureScript
var size is LumberSize = LumberSize.TWO_BY_FOUR;
```

--------------------------------

### Add conditional length parameter 'bumpHeight'

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Adds a conditional length parameter named 'bumpHeight' that is only visible when 'addBumps' is true. It uses LENGTH_BOUNDS for validation.

```FeatureScript
if (definition.addBumps)
{
    annotation { "Name" : "Bump height" }
    isLength(definition.bumpHeight, LENGTH_BOUNDS);
}
```

--------------------------------

### Onshape Manipulator Change Function

Source: https://cad.onshape.com/FsDoc/uispec

Defines the structure and purpose of the manipulator change function in Onshape custom features. This function allows dynamic modification of feature definitions based on user interaction with manipulators.

```typescript
export function onManipulatorChange(context is Context, definition is map, newManipulators is map) returns map
{
    // modify the definition based on what's in newManipulators
    ...

    return definition;
}
```

--------------------------------

### FeatureScript Unset Variables

Source: https://cad.onshape.com/FsDoc/variables

Shows how FeatureScript handles variables that are declared but not explicitly assigned a value. Such variables are automatically set to `undefined`.

```FeatureScript
var u;
println(u);             // prints "undefined"
```

--------------------------------

### Define Lumber Custom Type and Predicate

Source: https://cad.onshape.com/FsDoc/variables

Defines a custom type 'Lumber' and an associated predicate 'isLumber' to check if a value conforms to the Lumber type's structure and constraints. The predicate verifies if the value is a map, has a 'size' property of type 'LumberSize', and an 'length' property that satisfies 'isLength'.

```typescript
export type Lumber typecheck isLumber;

export predicate isLumber(value)
{
    value is map;
    value.size is LumberSize;
    isLength(value.length);
}
```

--------------------------------

### Set Default for Boolean Parameter

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Sets the default value for a boolean parameter in the UI. This is achieved using the 'Default' annotation.

```FeatureScript
annotation { "Name" : "Add bumps", "Default" : true }
definition.addBumps is boolean;
```

--------------------------------

### Subtract Body using Boolean Operation

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Performs a boolean subtraction operation to cut one body from another. It identifies the tool body (created by extrusion) and the target body (the part to be cut).

```FeatureScript
opBoolean(context, id + "boolean1", {
      "tools" : qCreatedBy(id + "extrude1", EntityType.BODY),
      "targets" : definition.partToCut,
      "operationType" : BooleanOperationType.SUBTRACTION
});
```

--------------------------------

### Narrow Slot Path Filter

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Modifies the filter for the slot path to be more specific, preventing unwanted input by filtering for edges on sketches and linear geometry.

```FeatureScript
annotation { "Name" : "Slot path", "Filter" : EntityType.EDGE && SketchObject.YES && GeometryType.LINE, "MaxNumberOfPicks" : 1 }
definition.slotPath is Query;
```

--------------------------------

### Assignment Statement

Source: https://cad.onshape.com/FsDoc/syntax

Modifies a variable or part of a variable. Supports scalar assignment, map/array element modification, and nested access.

```FeatureScript
a = 1;          // scalar
b.c = false;    // map
b['c'] = false; // same as previous
d[] = 0;        // box
e[0] = 0;       // map or array
f.g.h[] = 0;    // box in map in map
```

--------------------------------

### Query Sketch Region Containing Origin

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Selects the central region of a sketch that contains the coordinate system's origin. This is useful for isolating specific areas within a sketch for further operations.

```FeatureScript
var regionToExtrude = qContainsPoint(qSketchRegion(id + "sketch1"), cSys.origin);
debug(context, regionToExtrude);
```

--------------------------------

### FeatureScript Boolean Type

Source: https://cad.onshape.com/FsDoc/variables

Defines the boolean type in FeatureScript, which can hold one of two values: `true` or `false`.

```FeatureScript
var shouldCut = true;
```

--------------------------------

### Delete Sketch Bodies

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Removes sketch bodies that were used in the creation process. This helps to clean up the model by removing temporary or intermediate geometry.

```FeatureScript
opDeleteBodies(context, id + "delete1", {
      "entities" : qCreatedBy(id + "sketch1", EntityType.BODY)
});
```

--------------------------------

### Calculate X-axis Direction

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Calculates the X-axis direction by normalizing the vector between two positions. This is a common practice for defining the orientation of a coordinate system.

```FeatureScript
var xDirection is Vector = normalize(endPosition - startPosition);
```

--------------------------------

### Calculate Z-axis Direction

Source: https://cad.onshape.com/FsDoc/tutorials/add-sketch-geometry

Calculates the Z-axis direction by taking the normal of the owner sketch plane associated with an entity. This is used for defining the orientation of a coordinate system.

```FeatureScript
var zDirection is Vector = evOwnerSketchPlane(context, {
        "entity" : definition.slotPath
}).normal;
```

=== COMPLETE CONTENT === This response contains all available snippets from this library. No additional content exists. Do not make further requests.